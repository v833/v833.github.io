<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>前端安全：如何防范 XSS 和 CSRF</title>
      <link href="/2021/06/25/qian-duan-an-quan-ru-he-fang-fan-xss-he-csrf/"/>
      <url>/2021/06/25/qian-duan-an-quan-ru-he-fang-fan-xss-he-csrf/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="什么是 XSS 攻击？如何防范 XSS 攻击？">什么是 XSS 攻击？如何防范 XSS 攻击？</h2><br/><p>XSS 攻击指的是跨站脚本攻击，是一种<code>代码注入攻击</code>。攻击者通过在网站注入恶意脚本，使之在用户的浏览器上运行，从而盗取用户的信息如 <code>cookie</code> 等。</p><p>XSS 的本质是因为<code>网站没有对恶意代码进行过滤</code>，与正常的代码混合在一起了，浏览器没有办法分辨哪些脚本是可信的，从而导致了恶意代码的执行。</p><p>而由于直接在用户的终端执行，恶意代码能够直接获取用户的信息，或者利用这些信息冒充用户向网站发起攻击者定义的请求。</p><p class='p primary'>XSS 一般分为存储型、反射型和 DOM 型。</p><p>存储型指的是恶意代码提交到了网站的数据库中，当用户请求数据的时候，<code>服务器将其拼接为 HTML 后返回给了用户</code>，从而导致了恶意代码的执行。</p><p>反射型指的是攻击者构建了特殊的 URL，当服务器接收到请求后，从 <code>URL 中获取数据，拼接到 HTML 后返回</code>，从而导致了恶意代码的执行。</p><p>DOM 型指的是攻击者构建了特殊的 URL，用户打开网站后，<code>js 脚本从 URL 中获取数据，从而导致了恶意代码的执行</code>。</p><p class='p primary'>XSS 攻击的预防可以从两个方面入手，一个是恶意代码提交的时候，一个是浏览器执行恶意代码的时候。</p><p>对于第一个方面，如果我们对存入数据库的数据都进行的转义处理，但是一个数据可能在多个地方使用，有的地方可能不需要转义，由于我们没有办法判断数据最后的使用场景，<code>所以直接在输入端进行恶意代码的处理，其实是不太可靠的</code>。</p><p>因此我们可以从<code>浏览器的执行来进行预防</code>，一种是使用纯前端的方式，不用服务器端拼接后返回。另一种是对需要插入到 HTML 中的代码做好充分的转义。对于 DOM 型的攻击，主要是前端脚本的不可靠而造成的，我们对于<code>数据获取渲染和字符串拼接的时候应该对可能出现的恶意代码</code>情况进行判断。</p><p>在使用 <code>.innerHTML</code>、<code>.outerHTML</code>、<code>document.write()</code> 时要特别小心，不要把不可信的数据作为 HTML 插到页面上，而应尽量使用 <code>.textContent</code>、<code>.setAttribute()</code> 等。</p><p>如果用 Vuet 技术栈，并且不使用 <code>v-html</code>功能，就在前端 render 阶段避免 <code>innerHTML</code>、<code>outerHTML</code> 的 XSS 隐患。</p><p>DOM 中的内联事件监听器，如 <code>location</code>、<code>onclick</code>、<code>onerror</code>、<code>onload</code>、<code>onmouseover</code> 等，<code>&lt;a&gt;</code> 标签的 <code>href</code> 属性，JavaScript 的 <code>eval()</code>、<code>setTimeout()</code>、<code>setInterval()</code> 等，都能把字符串作为代码运行。如果不可信的数据拼接到字符串中传递给这些 API，很容易产生安全隐患，请务必避免。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- 内联事件监听器中包含恶意代码 --&gt;</span><br><span class="line">&lt;img onclick=&quot;UNTRUSTED&quot; onerror=&quot;UNTRUSTED&quot; src=&quot;data:image/png,&quot;&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- 链接内包含恶意代码 --&gt;</span><br><span class="line">&lt;a href=&quot;UNTRUSTED&quot;&gt;1&lt;/a&gt;</span><br><span class="line"></span><br><span class="line">&lt;script&gt;</span><br><span class="line">// setTimeout()/setInterval() 中调用恶意代码</span><br><span class="line">setTimeout(&quot;UNTRUSTED&quot;)</span><br><span class="line">setInterval(&quot;UNTRUSTED&quot;)</span><br><span class="line"></span><br><span class="line">// location 调用恶意代码</span><br><span class="line">location.href = &#x27;UNTRUSTED&#x27;</span><br><span class="line"></span><br><span class="line">// eval() 中调用恶意代码</span><br><span class="line">eval(&quot;UNTRUSTED&quot;)</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>输入内容长度控制</strong></p><p>对于不受信任的输入，都应该限定一个合理的长度。虽然无法完全防止 XSS 发生，但可以增加 XSS 攻击的难度。</p><p><strong>HTTP-only Cookie</strong></p><p>禁止 JavaScript 读取某些敏感 Cookie，攻击者完成 XSS 注入后也无法窃取此 Cookie。</p><p><strong>验证码</strong></p><p>防止脚本冒充用户提交危险操作。</p><p><strong>检测</strong></p><p> 只要在网站的各输入框中提交这个字符串，或者把它拼接到 URL 参数上，就可以进行检测了</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://xxx/search?keyword=jaVasCript%3A%2F*-%2F*%60%2F*%60%2F*%27%2F*%22%2F**%2F(%2F*%20*%2FoNcliCk%3Dalert()%20)%2F%2F%250D%250A%250d%250a%2F%2F%3C%2FstYle%2F%3C%2FtitLe%2F%3C%2FteXtarEa%2F%3C%2FscRipt%2F--!%3E%3CsVg%2F%3CsVg%2FoNloAd%3Dalert()%2F%2F%3E%3E</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="什么是 CSRF 攻击？如何防范 CSRF 攻击？">什么是 CSRF 攻击？如何防范 CSRF 攻击？</h2><br/><p>CSRF 攻击指的是跨站请求伪造攻击，攻击者诱导用户进入一个第三方网站，然后该网站向被攻击网站发送跨站请求。如果用户在被<br>攻击网站中保存了登录状态，那么攻击者就可以利用这个登录状态，绕过后台的用户验证，冒充用户向服务器执行一些操作。<br>CSRF 攻击的本质是利用了 cookie 会在同源请求中携带发送给服务器的特点，以此来实现用户的冒充。</p><p><strong>一个典型的CSRF攻击有着如下的流程：</strong></p><ul><li><p><a href="http://受害者登录a.com/">受害者登录a.com</a>，并保留了登录凭证（Cookie）。</p></li><li><p><a href="http://攻击者引诱受害者访问了b.com/">攻击者引诱受害者访问了b.com</a>。</p></li><li><p><a href="http://b.com/">b.com</a> 向 <a href="http://a.com/">a.com</a> 发送了一个请求：<a href="http://a.com/act=xx%E3%80%82%E6%B5%8F%E8%A7%88%E5%99%A8%E4%BC%9A%E9%BB%98%E8%AE%A4%E6%90%BA%E5%B8%A6a.com%E7%9A%84Cookie%E3%80%82">a.com/act=xx。浏览器会…</a></p></li><li><p>a.com接收到请求后，对请求进行验证，并确认是受害者的凭证，误以为是受害者自己发送的请求。</p></li><li><p>a.com以受害者的名义执行了act=xx。</p></li><li><p>攻击完成，攻击者在受害者不知情的情况下，冒充受害者，让a.com执行了自己定义的操作。</p><p><strong>CSRF的特点</strong></p><ul><li>攻击一般发起在第三方网站，而不是被攻击的网站。被攻击的网站无法防止攻击发生。</li><li>攻击利用受害者在被攻击网站的登录凭证，冒充受害者提交操作；而不是直接窃取数据。</li><li>整个过程攻击者并不能获取到受害者的登录凭证，仅仅是“冒用”。</li><li>跨站请求可以用各种方式：图片URL、超链接、CORS、Form提交等等。部分请求方式可以直接嵌入在第三方论坛、文章中，难以进行追踪。</li></ul><p>CSRF通常是跨域的，因为外域通常更容易被攻击者掌控。但是如果本域下有容易被利用的功能，比如可以发图和链接的论坛和评论区，攻击可以直接在本域下进行，而且这种攻击更加危险。</p></li></ul><p>一般的 CSRF 攻击类型有三种：</p><p class='p primary'>第一种是 GET 类型的 CSRF 攻击</p><p>比如在网站中的一个 img 标签里构建一个请求，当用户打开这个网站的时候就会自动发起提交。</p><p class='p primary'>第二种是 POST 类型的 CSRF 攻击</p><p>比如说构建一个表单，然后隐藏它，当用户进入页面时，自动提交这个表单。</p><p class='p primary'>第三种是链接类型的 CSRF 攻击</p><p>比如说在 a 标签的 href 属性里构建一个请求，然后诱导用户去点击。</p><p>CSRF 可以用下面几种方法来防护：</p><p class='p success'>第一种是同源检测的方法</p><p>服务器根据 http 请求头中 origin 或者 referer 信息来判断请求是否为允许访问的站点，从而对请求进行过滤。当 origin 或者 referer 信息都不存在的时候，直接阻止。这种方式的缺点是有些情况下 referer 可以被伪造。还有就是我们这种方法同时把搜索引擎的链接也给屏蔽了，所以一般网站会允许搜索引擎的页面请求，但是相应的页面请求这种请求方式也可能被攻击者给利用。</p><p class='p success'>第二种方法是使用 CSRF Token 来进行验证</p><p>服务器向用户返回一个随机数 Token ，当网站再次发起请求时，在请求参数中加入服务器端返回的 token ，然后服务器对这个 token 进行验证。这种方法解决了使用 cookie 单一验证方式时，可能会被冒用的问题，但是这种方法存在一个缺点就是，我们需要给网站中的所有请求都添加上这个 token，操作比较繁琐。还有一个问题是一般不会只有一台网站服务器，如果我们的请求经过负载平衡转移到了其他的服务器，但是这个服务器的 session 中没有保留这个 token 的话，就没有办法验证了。这种情况我们可以通过改变 token 的构建方式来解决。</p><p class='p success'>第三种方式使用双重 Cookie 验证的办法</p><p>服务器在用户访问网站页面时，向请求域名注入一个Cookie，内容为随机字符串，然后当用户再次向服务器发送请求的时候，从 cookie 中取出这个字符串，添加到 URL 参数中，然后服务器通过对 cookie 中的数据和参数中的数据进行比较，来进行验证。使用这种方式是利用了攻击者只能利用 cookie，但是不能访问获取 cookie 的特点。并且这种方法比 CSRF Token 的方法更加方便，并且不涉及到分布式访问的问题。这种方法的缺点是如果网站存在 XSS 漏洞的，那么这种方式会失效。同时这种方式不能做到子域名的隔离。</p><p class='p success'>第四种方式是使用在设置 cookie 属性的时候设置 Samesite</p><p> 限制 cookie 不能作为被第三方使用，从而可以避免被攻击者利用。Samesite 一共有两种模式，一种是严格模式，在严格模式下 cookie 在任何情况下都不可能作为第三方 Cookie 使用，在宽松模式下，cookie 可以被请求是 GET 请求，且会发生页面跳转的请求所使用。</p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 安全 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ES6 的 Reflect</title>
      <link href="/2021/06/24/es6-de-reflect/"/>
      <url>/2021/06/24/es6-de-reflect/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="Reflect概述">Reflect概述</h2><br/><blockquote><p>什么是<code>Reflect</code>对象？</p><p>我们可以这样理解，有一个名为<code>Reflect的全局对象</code>，上面挂载了对象的某些特殊函数(如：<code>[[Get]]</code>、<code>[[Set]]</code>)，这些函数可以通过类似于<code>Reflect.apply()</code>这种形式调用，所有在Reflect对象上的函数要么可以在<code>Object原型链</code>中找到，要么可以通过<code>命令式操作符</code>实现，例如<code>delete</code>和<code>in</code>操作符。</p></blockquote><p><em><strong>既然在ES6以前，Object对象中已经有与Reflect的函数相同功能的函数或者命令式操作符，那么为什么还要在ES6中专门增加一个Reflect对象呢？</strong></em></p><p>主要原因有以下几点。</p><ul><li><p><code>更合理地规划于Object对象相关的API</code><br>在ES6中，Object对象的一些明显属于<strong>语言内部的函数</strong>都会添加到Reflect对象中，这样Object对象与Reflect对象中会存在相同处理函数。<strong>而在未来的设计中，语言内部的函数只会添加到Reflect对象中</strong>。</p></li><li><p><code>全局对象(内置的对象)</code><br>用一个单一的全局对象去存储这些函数，能够保持其他的Javascript代码的整齐、干净。不然的话，这些函数可能是全局的，或者通过原型来调用，<strong>不方便统一管理</strong>。</p></li><li><p><code>代替命令式操作符</code><br>将一些命令式操作符如delete、in等使用函数来代替，这样的目的是为了<strong>代码更好维护，更好向下兼容</strong>，同时避免出现更多的保留字。</p></li></ul><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 传统写法</span></span><br><span class="line"><span class="string">&#x27;toString&#x27;</span> <span class="keyword">in</span> <span class="built_in">Object</span> <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 新写法</span></span><br><span class="line"><span class="built_in">Reflect</span>.has(<span class="built_in">Object</span>, <span class="string">&#x27;toString&#x27;</span>) <span class="comment">// true</span></span><br></pre></td></tr></table></figure><ul><li><code>修改Object对象的某些函数的返回结果</code><br>修改Object对象的某些函数的返回结果，可以让其变得更合理，使得代码更好维护。</li></ul><p>如果一个对象obj是<strong>不能扩展的</strong>，那么在调用ObjectdefineProperty(obj, prop, desc)时，会<strong>抛出一个异常</strong>。因此在传统的写法中，我们需要通过try…catch处理。</p><p>而使用Reflect.defineProperty(obj, prop, desc)时，返回的是<strong>false</strong>，新写法就可以通过if…else实现。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 传统写法</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  <span class="built_in">Object</span>.defineProperty(obj, prop, desc) </span><br><span class="line">    <span class="comment">// success</span></span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="comment">// failure</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新写法</span></span><br><span class="line"><span class="keyword">if</span> ( <span class="built_in">Reflect</span>.defineProperty(obj, prop, desc) ) &#123;</span><br><span class="line">  <span class="comment">// success</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// failure</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><code>Reflect对象的函数与Proxy对象的函数一一对应</code></li></ul><p><strong>只要是Proxy对象的函数，就能在Reflect对象上找到对应的函数</strong>。这就让Proxy对象可以方便的调用对应的Reflect对象上的函数，完成默认行为，并以此为修改行为的基础。</p><p>也就是说，不管Proxy对象怎么修改默认行为，总可以在Refect对象上获取默认行为。事实上<strong>Proxy对象也经常随着Reflect对象一起进行调用</strong>。</p><h2 class="bamboo-h " id="Reflect静态函数">Reflect静态函数</h2><br/><p>**Reflect对象本身并不是一个构造函数(无法通过 new 调用)**，而是直接提供静态函数以供调用，Reflect对象的静态函数一共13个，如下所示。</p><ul><li><p><code>Reflect.get(target, prop, receiver)</code><br>Reflect.get()函数的作用是<strong>获取对象的属性值</strong>，等同于执行target[prop]<br>receiver表示函数中this绑定的对象。</p></li><li><p><code>Reflect.set(target, prop, value, receiver)</code><br>Reflect.set()的作用是<strong>设置某个属性值</strong>。等同于执行target[prop] = value。</p></li><li><p><code>Reflect.apply(target, thisArg, args)</code><br>Reflect.apply()函数的作用是<strong>通过指定的参数列表执行target函数</strong>，等同于执行Function.prototype.apply.call(target, thisArg, args)。其中target表示的是目标对象，thsArg表示的是执行target函数时的this对象，args表示的时参数列表。</p></li><li><p><code>Reflect.has(target, prop)</code><br>Reflect.has()函数的作用是<strong>判断属性是否在对象中</strong>，等同于执行prop in target</p></li></ul><ul><li><p><code>Reflect.deleteProperty(target, prop)</code><br>Reflect.deleteProperty()函数的作用是<strong>删除对象的属性</strong>，等同于执行delete obj[prop]</p></li><li><p><code>Reflect.ownKeys(target)</code><br>Reflect.ownKeys()函数的作用是<strong>获取对象的所有属性</strong>，包括Symbol，等同于Object.getOwnPrototypeNames()与Objct.getOwnPrototypeSymbols()之和。</p></li><li><p><code>Reflect.construct(target, args)</code><br>Reflect.construct()函数的作用是<strong>执行构造函数</strong>，等同于执行new Target(…args)</p></li><li><p><code>Reflect.defineProperty(target, prop, desc)</code><br>Reflect.defineProperty()函数的作用是<strong>为对象定义属性</strong>，等同于执行Object.defineProperty()</p></li></ul><ul><li><p><code>Reflect.getOwnPropertyDescriptor(target, prop)</code><br>Reflect.getOwnPropertyDescriptor()函数的作用是<strong>得到制定属性的描述对象</strong>，等同于执行Object.getOwnPropertyDescriptor()</p></li><li><p><code>Reflect.setPrototypeOf(target, newProto)</code><br>Reflect.setPrototypeOf()函数的作用是<strong>设置对象的原型</strong>，等同于执行Object.setPrototypeOf(target, newProto)</p></li><li><p><code>Reflect.getPrototypeOf(target)</code><br>Reflect.getPrototypeOf()函数的作用是<strong>读取对象的__proto__属性</strong>，等同于执行Object.getPrototypeOf(obj)</p></li></ul><ul><li><p><code>Reflect.isExtensible(target)</code><br>Reflect.isExtensible()函数的作用是<strong>判断对象是否可拓展</strong>，等同于执行Object.isExtensible()</p></li><li><p><code>Reflect.preventExtensions(target)</code><br>Reflect.preventExtensions()函数的作用是<strong>让一个函数变得不可拓展</strong>，等同于执行Object.preventExtensions()</p></li></ul><blockquote><p>Reflect 上存在的一些静态函数对应于ES2015之前的Object上可用的方法。尽管某些方法在行为上看似相似，但它们之间常常存在细微的差异。</p><p><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Reflect/Comparing_Reflect_and_Object_methods">查看具体差异(MDN)</a></p></blockquote><h2 class="bamboo-h " id="Reflect 与 Proxy">Reflect 与 Proxy</h2><br/><p><strong>ES6在设计的时候就将Reflect和Proxy对象绑定在一起了</strong>，Reflect对象的函数与Proxy对象的函数一一对应，因此在Proxy对象中调用Reflect对象对应的函数是一个明智的选择。</p><p>例如我们使用Proxy对象拦截属性的读取、设置和删除操作，并配合Reflect对象实现时，可以编写如下代码。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> target = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">target = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log( <span class="string">`读取属性<span class="subst">$&#123;prop&#125;</span>的值为<span class="subst">$&#123;target[prop]&#125;</span>`</span> )</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(target, prop)</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="title">set</span>(<span class="params">target, prop, value</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log( <span class="string">`设置属性<span class="subst">$&#123;prop&#125;</span>的值为<span class="subst">$&#123;value&#125;</span>`</span> )</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.set(target, prop, value)</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="title">deleteProperty</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log( <span class="string">`删除属性<span class="subst">$&#123;prop&#125;</span>`</span> )</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.deleteProperty(target, prop)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">target.name <span class="comment">// 读取属性name的值为xx</span></span><br><span class="line">target.name = <span class="string">&#x27;yy&#x27;</span> <span class="comment">// 设置属性name的值为yy</span></span><br><span class="line"><span class="keyword">delete</span> target.name <span class="comment">// 删除属性name</span></span><br></pre></td></tr></table></figure><blockquote><p>上面的实例只是讲解了如何配合使用Proxy对象和Reflect对象，那么两者的配合能实现什么样的功能呢？</p></blockquote><p><strong>有一个最经典的案例就是可以实现观察者模式</strong>。</p><h3 class="bamboo-h warning" id="Proxy 和 Reflect 实现观察者模式">Proxy 和 Reflect 实现观察者模式</h3><br/><blockquote><p>观察者模式的表现</p><p>一个目标对象管理所有依赖它的观察者对象</p><p>当自身的状态有改变时，会主动向所有观察者发出通知</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">makeObservable</span> (<span class="params">target</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 初始化 handler 存储</span></span><br><span class="line">  target.observers = <span class="keyword">new</span> <span class="built_in">Set</span>()</span><br><span class="line"></span><br><span class="line">  <span class="comment">//将 handler 函数存储到 Set 中，以便于之后调用</span></span><br><span class="line">  target.observe = <span class="function"><span class="params">handler</span> =&gt;</span> target.observers.add(handler)</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 创建一个 proxy 以处理更改</span></span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Proxy</span> (target, &#123;</span><br><span class="line">    <span class="function"><span class="title">set</span>(<span class="params">target, prop, value</span>)</span> &#123;</span><br><span class="line">      <span class="comment">// 将操作转发给对象</span></span><br><span class="line">      <span class="keyword">let</span> success = <span class="built_in">Reflect</span>.set(...arguments)</span><br><span class="line">      <span class="keyword">if</span> (success) &#123;</span><br><span class="line">        <span class="comment">// 调用所有 handler</span></span><br><span class="line">        target.observers.forEach(<span class="function"><span class="params">handler</span> =&gt;</span> handler(prop, value))</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> success</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> user = &#123;&#125;;</span><br><span class="line">user = makeObservable(user)</span><br><span class="line"><span class="comment">// 定义一个观察者</span></span><br><span class="line">user.observe(<span class="function">(<span class="params">key, value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`SET <span class="subst">$&#123;key&#125;</span>=<span class="subst">$&#123;value&#125;</span>`</span>)</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">user.name = <span class="string">&quot;xx&quot;</span> <span class="comment">// SET name=xx</span></span><br><span class="line">user.age = <span class="number">24</span> <span class="comment">// SET age=24</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="代理一个 getter">代理一个 getter</h3><br/><p>让我们看一个示例，来说明为什么 <strong>Reflect.get 更好</strong>。此外，<strong>我们还将看到为什么 get/set 有第三个参数 receiver，而且我们之前从来没有使用过它。</strong></p><p>我们有一个带有 _name 属性和 getter 的对象 user。</p><p>这是对 user 对象对一个代理（proxy）：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">_name</span>: <span class="string">&quot;xx&quot;</span>,</span><br><span class="line">  <span class="keyword">get</span> <span class="title">name</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>._name;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> userProxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(user, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop, receiver</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> target[prop];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(userProxy.name) <span class="comment">// xx</span></span><br></pre></td></tr></table></figure><blockquote><p>另一个对象 admin 从 user 继承后，我们可以观察到错误的行为</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">_name</span>: <span class="string">&quot;xx&quot;</span>,</span><br><span class="line">  <span class="keyword">get</span> <span class="title">name</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>._name;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> userProxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(user, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop, receiver</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> target[prop]  <span class="comment">// (*) target = user</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> admin = &#123;</span><br><span class="line">  <span class="attr">__proto__</span>: userProxy,</span><br><span class="line">  <span class="attr">_name</span>: <span class="string">&quot;yy&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 期望输出：yy</span></span><br><span class="line"><span class="built_in">console</span>.log(admin.name); <span class="comment">// 输出：xx (!??)</span></span><br></pre></td></tr></table></figure><p><strong>读取 admin.name 应该返回 “yy”，而不是 “xx”！</strong></p><p>发生了什么？或许我们在继承方面做错了什么？</p><p>但是，如果我们移除代理，那么一切都会按预期进行。</p><p>问题实际上出在代理中，<strong>在 (*) 行。</strong></p><p>当我们读取 admin.name 时，由于 admin 对象自身没有对应的的属性，搜索将转到其原型。</p><p>原型是 userProxy。</p><p>从代理读取 name 属性时，get 捕捉器会被触发，并从原始对象返回 target[prop] 属性，在 (*) 行。</p><p>当调用 target[prop] 时，若 prop 是一个 getter，它将在 <strong>this=target 上下文中运行其代码</strong>。因此，结果是来自原始对象 target 的 this._name，即来自 <strong>user</strong>。</p><p>为了解决这种情况，我们需要 get 捕捉器的第三个参数 receiver。它保证将正确的 this 传递给 getter。在我们的例子中是 admin。</p><blockquote><p>如何把上下文传递给 getter？</p><p>对于一个常规函数，我们可以使用 call/apply，但这是一个 <strong>getter</strong>，它不能“被调用”，只能被访问。</p></blockquote><p>Reflect.get 可以做到。如果我们使用它，一切都会正常运行。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">_name</span>: <span class="string">&quot;xx&quot;</span>,</span><br><span class="line">  <span class="keyword">get</span> <span class="title">name</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>._name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> userProxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(user, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop, receiver</span>)</span> &#123; <span class="comment">// receiver = admin</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(target, prop, receiver) <span class="comment">// (*)</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> admin = &#123;</span><br><span class="line">  <span class="attr">__proto__</span>: userProxy,</span><br><span class="line">  <span class="attr">_name</span>: <span class="string">&quot;yy&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(admin.name) <span class="comment">// yy</span></span><br></pre></td></tr></table></figure><p>现在 receiver 保留了对正确 this 的引用（即 admin），该引用是在 (*) 行中被通过 Reflect.get 传递给 getter 的。</p><p><strong><em>我们可以把捕捉器重写得更短</em>：</strong></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="title">get</span>(<span class="params">target, prop, receiver</span>)</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(...arguments);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ES6 的 Proxy</title>
      <link href="/2021/06/23/es6-de-proxy/"/>
      <url>/2021/06/23/es6-de-proxy/</url>
      
        <content type="html"><![CDATA[<blockquote><p>ES6中新增了Proxy对象，从字面量上看可以理解为代理器，主要用于<strong>改变对象的默认访问行为</strong>，实际表现是在访问对象之间增加了一层<strong>拦截</strong>，任何对对象的访问行为都会通过这层拦截。在拦截中，我们可以<strong>增加自定义的行为</strong>。</p></blockquote><h3 class="bamboo-h warning" id="Proxy的基本语法：">Proxy的基本语法：</h3><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler)</span><br></pre></td></tr></table></figure><p>target —— 是要包装的对象，可以是任何东西，包括函数。</p><p>handler —— 代理配置：带有“捕捉器”（“traps”，即拦截操作的方法）的对象。比如 get 捕捉器用于读取 target 的属性，set 捕捉器用于写入 target 的属性，等等。</p><p><strong>对 proxy 进行操作，如果在 handler 中存在相应的捕捉器，则它将运行</strong>，并且 Proxy 有机会对其进行处理，否则将直接对 target 进行处理。</p><h3 class="bamboo-h warning" id="Proxy实例">Proxy实例</h3><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//我们用 get 来实现一个对象的默认值。</span></span><br><span class="line"><span class="comment">//我们将创建一个对不存在的数组项返回 0 的数组。</span></span><br><span class="line"><span class="comment">//通常，当人们尝试获取不存在的数组项时，他们会得到 undefined，</span></span><br><span class="line"><span class="comment">//但是我们在这将常规数组包装到代理（proxy）中，以捕获读取操作，并在没有要读取的属性的时返回 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义目标数组</span></span><br><span class="line"><span class="keyword">let</span> numbers = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义配置对象</span></span><br><span class="line"><span class="keyword">let</span> handler = &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> prop <span class="keyword">in</span> target ? target[prop] : <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 生成Proxy实例</span></span><br><span class="line"><span class="keyword">let</span> numbersProxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(numbers, handler)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行结果</span></span><br><span class="line"><span class="built_in">console</span>.log(numbersProxy[<span class="number">1</span>]); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(numbersProxy[<span class="number">123</span>]); <span class="comment">// 0（没有这个数组项）</span></span><br></pre></td></tr></table></figure><p>上面实例中，我们定义了一个包含get()函数的配置对象，表示的是对代理对象的属性进行读取操作，就会触发get()函数。因此在执行numbersProxy[1]，即访问Proxy实例数组项为1的值，会触发get()函数，在控制台输出自定义的结果。</p><h3 class="bamboo-h warning" id="使用Proxy时的注意事项">使用Proxy时的注意事项</h3><br/><blockquote><p><strong>必须通过代理实例访问</strong></p><p>如果需要配置对象的拦截行为生效，那么必须是对代理实例的属性进行访问，而不是直接对目标对象进行访问。</p></blockquote><h3 class="bamboo-h warning" id="Proxy实例函数及其基本使用">Proxy实例函数及其基本使用</h3><br/><p>在上面例子中，我们通过访问代理对象的属性来触发自定义配置对象的get()函数。而get()函数只是Proxy实例支持的总共13中函数中的一种，这13中函数汇总如下。</p><h4 class="bamboo-h red" id="get(target，property，receiver)">get(target，property，receiver)</h4><br/><p>用于拦截对象的读取属性操作。例如调用proxy.name或者proxy[name]，其中target表示的是目标对象，property表示的是读取的属性值，receiver表示的是配置对象(最初被调用的对象。通常是 proxy 本身)。</p><h4 class="bamboo-h red" id="set(target，property，value，receiver)">set(target，property，value，receiver)</h4><br/><p>拦截对象属性的写入操作，即设置属性值，例如proxy.name = ‘xx’或者proxy[name] = ‘xx’，其中target表示目标对象，property表示的是将要设置的属性，value表示将要设置的属性值，receiver表示的是配置对象。</p><h4 class="bamboo-h red" id="has(target，prop)">has(target，prop)</h4><br/><p>拦截hasProperty的操作，返回一个布尔值，最典型的表现形式是执行prop in target，其中target表示目标对象，prop表示判断的属性值。</p><h4 class="bamboo-h red" id="deleteProperty(target，property)">deleteProperty(target，property)</h4><br/><p>拦截delete proxy[property]的操作，返回一个布尔值，表示是否执行成功，其中target表示目标对象，property表示将要删除的属性。</p><h4 class="bamboo-h red" id="ownkeys(target)">ownkeys(target)</h4><br/><p>拦截Object.getOwnPropertyNames(target)、Object.getOwnPropertySymbols(target)、Object.keys(target)、for…in循环等操作，其中target表示的是获取对象自身所有的属性名。</p><h5 class="bamboo-h " id="拓展 ,">拓展 ,</h5><br/><p>Object.getOwnPropertyNames(obj) 返回非 Symbol 键。</p><p>Object.getOwnPropertySymbols(obj) 返回 Symbol 键。</p><p>Object.keys/values() 返回带有 enumerable 标志的非 Symbol 键/值。</p><p>for..in 循环遍历所有带有 enumerable 标志的非 Symbol 键，以及原型对象的键。</p><h4 class="bamboo-h red" id="getOwnPropertyDecriptor(target，prop)">getOwnPropertyDecriptor(target，prop)</h4><br/><p>拦截Object.getOwnPropertyDecriptor(proxy, prop)操作，返回属性的属性描述符构成的对象，其中target表示目标对象，prop表示需要获取属性描述符集合的属性。</p><h4 class="bamboo-h red" id="defineProperty(target，property，descriptor)">defineProperty(target，property，descriptor)</h4><br/><p>拦截Object.defineProperty(proxy, property, decriptor)、Object.defineProperties(proxy, decriptors)操作，返回一个布尔值，其中target表示目标对象，property表示新增的属性，descriptor表示的是属性描述符对象。</p><h4 class="bamboo-h red" id="preventExtensions(target)">preventExtensions(target)</h4><br/><p>拦截Object.preventExtensions(proxy)操作，返回一个布尔值，表示的是让一个对象变得不可扩展，不能再增加新属性，其中target表示目标对象。</p><h4 class="bamboo-h red" id="isExtensible(target)">isExtensible(target)</h4><br/><p>拦截Object.isExtensible(proxy)，返回一个布尔值，表示对象是否可拓展。其中target表示目标对象。</p><h4 class="bamboo-h red" id="getPrototypeOf(target)">getPrototypeOf(target)</h4><br/><p>拦截Object.getPrototypeOf(proxy)操作，返回一个对象，表示的是拦截获取对象原型属性，其中target表示目标对象。</p><h4 class="bamboo-h red" id="setPropertyOf(target，prototype)">setPropertyOf(target，prototype)</h4><br/><p>拦截Object.setPrototype(proxy, prototype)操作，返回一个布尔值，表示的是拦截设置对象的原型属性的行为，其中target表示目标对象，prototype表示新的原型对象。</p><h4 class="bamboo-h red" id="apply(target，object，args)">apply(target，object，args)</h4><br/><p>拦截Proxy实例作为函数调用的操作，例如proxy(…args)、proxy.call(object, …args)、proxy.apply(…)，其中target表示目标对象，object表示函数的调用方，args表示函数调用传递的参数。</p><h4 class="bamboo-h red" id="constructor(target，args)">constructor(target，args)</h4><br/><p>拦截Proxy实例作为构造函数调用的操作，例如new Proxy(…args)，其中target表示目标对象，args表示函数调用传递的参数。</p><blockquote><p>这些函数都有一个通用的特性，即如果再target中使用了this关键字，再通过Proxy处理后，<strong>this关键之指向的是Proxy实例，而不是目标对象target</strong>。</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> person = &#123;</span><br><span class="line">  <span class="function"><span class="title">getName</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span> === proxy)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(person, &#123;&#125;)</span><br><span class="line">person.getName() <span class="comment">// fasle</span></span><br><span class="line">proxy.getName() <span class="comment">// true</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="Proxy应用场景">Proxy应用场景</h3><br/><h4 class="bamboo-h red" id="读取不存在属性">读取不存在属性</h4><br/><p>通常，尝试读取不存在的属性会返回 undefined。</p><p>创建一个代理，在尝试读取不存在的属性时，该代理抛出一个错误。</p><p>这可以帮助及早发现编程错误。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;xx&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">wrap</span>(<span class="params">target</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, &#123;</span><br><span class="line">    <span class="function"><span class="title">get</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (prop <span class="keyword">in</span> target) &#123;</span><br><span class="line">        <span class="keyword">return</span> target[prop]</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">ReferenceError</span>(<span class="string">`Property doesn&#x27;t exist: &quot;<span class="subst">$&#123;prop&#125;</span>&quot;`</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">user = wrap(user);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(user.name); <span class="comment">// xx</span></span><br><span class="line"><span class="built_in">console</span>.log(user.age); <span class="comment">// ReferenceError: Property doesn&#x27;t exist: &quot;age&quot;</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="读取负索引的值">读取负索引的值</h4><br/><p>数组的索引值时从0开始依次递增的，正常情况下我们无法读取负索引的值，但是通过<strong>Proxy的get()函数</strong>可以做到这一点。</p><blockquote><p>负索引实际就是从数组的尾部元素开始，从后往前，寻找元素的位置。</p><p><strong>换句话说，array[-N] 与 array[array.length - N] 相同。</strong></p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</span><br><span class="line">arr = <span class="keyword">new</span> <span class="built_in">Proxy</span>(arr, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, index</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> target[index]</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> target[+index + target.length]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(arr[<span class="number">1</span>]) <span class="comment">// 2</span></span><br><span class="line"><span class="built_in">console</span>.log(arr[<span class="number">2</span>]) <span class="comment">// 3</span></span><br><span class="line"><span class="built_in">console</span>.log(arr[-<span class="number">1</span>]) <span class="comment">// 5</span></span><br><span class="line"><span class="built_in">console</span>.log(arr[-<span class="number">2</span>]) <span class="comment">// 4   </span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="禁止访问私有属性">禁止访问私有属性</h4><br/><p>在一些约定熟成的写法中，私有属性都会以下划线(_)开头，事实上，我们并不希望用户能访问到私有属性。</p><p><strong><em>我们将需要以下捕捉器</em>：</strong></p><p><code>get 读取此类属性时抛出错误</code></p><p><code>set 写入属性时抛出错误</code></p><p><code>deleteProperty 删除属性时抛出错误</code></p><p><code>has 在使用 in 方法时排除以 _ 开头的属性</code></p><p><code>ownKeys 在使用 for..in 和像 Object.keys 这样的的方法时排除以 _ 开头的属性</code></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;xx&quot;</span>,</span><br><span class="line">  <span class="attr">_age</span>: <span class="string">&quot;18&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">user = <span class="keyword">new</span> <span class="built_in">Proxy</span>(user, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop</span>)</span> &#123; <span class="comment">// 拦截属性读取</span></span><br><span class="line">    <span class="keyword">if</span> (prop.startsWith(<span class="string">&#x27;_&#x27;</span>)) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;Access denied&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> target[prop]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">set</span>(<span class="params">target, prop, val</span>)</span> &#123; <span class="comment">// 拦截属性写入</span></span><br><span class="line">    <span class="keyword">if</span> (prop.startsWith(<span class="string">&#x27;_&#x27;</span>)) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;Access denied&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      target[prop] = val</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">deleteProperty</span>(<span class="params">target, prop</span>)</span> &#123; <span class="comment">// 拦截属性删除</span></span><br><span class="line">    <span class="keyword">if</span> (prop.startsWith(<span class="string">&#x27;_&#x27;</span>)) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;Access denied&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">delete</span> target[prop]</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">has</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (prop.startsWith(<span class="string">&#x27;_&#x27;</span>)) &#123; <span class="comment">// 拦截 in 操作</span></span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;Access denied&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> prop <span class="keyword">in</span> target</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">ownKeys</span>(<span class="params">target</span>)</span> &#123; <span class="comment">// 拦截读取属性列表</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Object</span>.keys(target).filter(<span class="function"><span class="params">key</span> =&gt;</span> !key.startsWith(<span class="string">&#x27;_&#x27;</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// &quot;get&quot; 不允许读取 _age</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  user._age <span class="comment">// Error: Access denied</span></span><br><span class="line">&#125; <span class="keyword">catch</span>(e) &#123; <span class="built_in">console</span>.log(e.message) &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &quot;set&quot; 不允许写入 _age</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  user._age = <span class="string">&quot;20&quot;</span> <span class="comment">// Error: Access denied</span></span><br><span class="line">&#125; <span class="keyword">catch</span>(e) &#123; <span class="built_in">console</span>.log(e.message) &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &quot;deleteProperty&quot; 不允许删除 _age</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  <span class="keyword">delete</span> user._age <span class="comment">// Error: Access denied</span></span><br><span class="line">&#125; <span class="keyword">catch</span>(e) &#123; <span class="built_in">console</span>.log(e.message) &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &quot;has&quot; 将 _age 过滤出去</span></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="string">&#x27;_age&#x27;</span> <span class="keyword">in</span> user</span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123; <span class="built_in">console</span>.log( e.message ) &#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">// &quot;ownKeys&quot; 将 _age 过滤出去</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> key <span class="keyword">in</span> user) <span class="built_in">console</span>.log(key) <span class="comment">// name</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="Proxy访问属性的限制">Proxy访问属性的限制</h4><br/><p>当我们期望使用Proxy对对象的属性进行代理，并修改属性的返回值时，我们需要这个属性<strong>不能同时为不可配置和不可写</strong>。如果这个属性同时为不可配置和不可写，那么在通过代理读取属性时，会抛出异常。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> target = <span class="built_in">Object</span>.defineProperties(&#123;&#125;, &#123;</span><br><span class="line">  <span class="comment">// 可配置的name</span></span><br><span class="line">  <span class="attr">name</span>: &#123;</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&#x27;xx&#x27;</span>,</span><br><span class="line">    <span class="attr">configurable</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">writable</span>: <span class="literal">false</span></span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 不可配置的age</span></span><br><span class="line">  <span class="attr">age</span>: &#123;</span><br><span class="line">    <span class="attr">value</span>: <span class="number">18</span>,</span><br><span class="line">    <span class="attr">configurable</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">writable</span>: <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">target = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, &#123;</span><br><span class="line">  <span class="function"><span class="title">get</span>(<span class="params">target, prop</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;abc&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(target.name) <span class="comment">// abc</span></span><br><span class="line"><span class="built_in">console</span>.log(target.age) <span class="comment">// Uncaught TypeError: &#x27;get&#x27; on proxy: property &#x27;age&#x27; is a read-only and non-configurable data property on the proxy target but the proxy did not return its actual value (expected &#x27;18&#x27; but got &#x27;abc&#x27;)</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="拦截属性赋值操作">拦截属性赋值操作</h4><br/><p>定义一个person对象，包含一个age属性，取值在0~100之间，只要设置的值不在区间内，就会抛出异常。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span>,</span><br><span class="line">  <span class="attr">age</span>: <span class="number">18</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">user = <span class="keyword">new</span> <span class="built_in">Proxy</span>(user, &#123;</span><br><span class="line">  <span class="function"><span class="title">set</span>(<span class="params">target, prop, number</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (prop === <span class="string">&#x27;age&#x27;</span> &amp;&amp; number &gt; <span class="number">100</span> || number &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;The age is invalid&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    target[prop] = number</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">user.name = <span class="string">&#x27;yy&#x27;</span></span><br><span class="line"><span class="built_in">console</span>.log(user.name) <span class="comment">// yy</span></span><br><span class="line">user.age = <span class="number">20</span></span><br><span class="line"><span class="built_in">console</span>.log(user.age) <span class="comment">// 20</span></span><br><span class="line">user.age = <span class="number">200</span></span><br><span class="line"><span class="built_in">console</span>.log(user.age) <span class="comment">// Uncaught Error: The age is invalid</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="函数的拦截">函数的拦截</h4><br/><p>Proxy中提供了apply()函数，用于拦截函数调用的操作，<strong>函数调用包括直接调用、call()函数调用、apply()函数调用<code>3</code>种方式</strong>。</p><p>通过对函数调用的拦截，可以加入自定义操作，从而得到新的函数处理结果。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sum</span> (<span class="params">num1, num2</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> num1 + num2</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">sum = <span class="keyword">new</span> <span class="built_in">Proxy</span>(sum, &#123;</span><br><span class="line">  <span class="function"><span class="title">apply</span>(<span class="params">target, obj, args</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> target.apply(obj, args) * <span class="number">2</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( sum(<span class="number">1</span>, <span class="number">2</span>) ) <span class="comment">// 6</span></span><br><span class="line"><span class="built_in">console</span>.log( sum.call(<span class="literal">null</span>, <span class="number">1</span>, <span class="number">2</span>) ) <span class="comment">// 6</span></span><br><span class="line"><span class="built_in">console</span>.log( sum.apply(<span class="literal">null</span>, [<span class="number">1</span>, <span class="number">2</span>]) ) <span class="comment">// 6</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>性能优化：防抖和节流</title>
      <link href="/2021/06/20/xing-neng-you-hua-fang-dou-he-jie-liu/"/>
      <url>/2021/06/20/xing-neng-you-hua-fang-dou-he-jie-liu/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="debounce防抖">debounce防抖</h2><br/><p><code>debounce(f, ms)</code>装饰器的结果是一个包装器，该包装器将暂停对 <code>f</code> 的调用，直到经过 <code>ms</code> 毫秒的非活动状态（没有函数调用，“冷却期”），然后使用最新的参数调用 <code>f</code> 一次。</p><blockquote><p>换句话说，<code>debounce</code> 就像一个“接听电话”的秘书，并一直等到 <code>ms</code> 毫秒的安静时间之后，才将最新的呼叫信息传达给“老板”（调用实际的 <code>f</code>）。</p></blockquote><p>举个例子，我们有一个函数 <code>f</code>，并将其替换为 <code>f = debounce(f, 1000)</code>。</p><p>然后，如果包装函数分别在 <code>0ms、200ms 和 500ms</code> 时被调用了，之后没有其他调用，那么实际的 <code>f</code> 只会在 <code>1500ms</code> 时被调用一次。也就是说：从最后一次调用开始经过 <code>1000ms</code> 的冷却期之后。</p><p><img "" class="lazyload placeholder" data-original="/medias/debounce.png" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="throttle"></p><p>……并且，它将获得最后一个调用的所有参数，其他调用的参数将被忽略。</p><h3 class="bamboo-h warning" id="防抖举例">防抖举例</h3><br/><p>现在我们举一个实际中的例子。假设用户输入了一些内容，我们想要在用户输入完成时向服务器发送一个请求。</p><p>我们没有必要为每一个字符的输入都发送请求。相反，我们想要等一段时间，然后处理整个结果。</p><p>在 <code>Web</code> 浏览器中，我们可以设置一个事件处理程序 —— 一个在每次输入内容发生改动时都会调用的函数。通常，监听所有按键输入的事件的处理程序会被调用的非常频繁。但如果我们为这个处理程序做一个 <code>1000ms</code> 的 <code>debounce</code> 处理，它仅会在最后一次输入后的 <code>1000ms</code> 后被调用一次。</p><p><img "" class="lazyload placeholder" data-original="/medias/debounce.gif" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="debounce"></p><p>看到了吗？第二个输入框调用了防抖函数，所以它的内容是在最后一次输入的 <code>1000ms</code> 后被处理的。</p><p>因此，<code>debounce</code> 是一个处理一系列事件的好方法：无论是系列键盘输入，鼠标移动还是其他类似的事件。</p><p>它在最后一次调用之后等待给定的时间，然后运行其可以处理结果的函数。</p><h3 class="bamboo-h warning" id="实现一个防抖函数">实现一个防抖函数</h3><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">debounce</span>(<span class="params">f, ms</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> timeout;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">clearTimeout</span>(timeout);</span><br><span class="line">    timeout = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> f.apply(<span class="built_in">this</span>, <span class="built_in">arguments</span>), ms);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>调用 debounce 会返回一个包装器。当它被调用时，它会安排一个在给定的 ms 之后对原始函数的调用，并取消之前的此类超时。</p></blockquote><h2 class="bamboo-h " id="throttle节流">throttle节流</h2><br/><p>当被多次调用时，它会在每 <code>ms</code> 毫秒最多将调用传递给 <code>f</code> 一次。</p><p>与防抖抖的不同是，它是个完全不同的装饰器：</p><p><code>debounce</code> 会在“冷却”期后运行函数一次。适用于处理最终结果。<br><code>throttle</code> 运行函数的频率不会大于所给定的时间 <code>ms</code> 毫秒。适用于不应该经常进行的定期更新。</p><blockquote><p>换句话说，throttle 就像接电话的秘书，但是打扰老板（实际调用 <code>f</code>）的频率不能超过每 <code>ms</code> 毫秒一次。</p></blockquote><h3 class="bamboo-h warning" id="节流举例">节流举例</h3><br/><p><img "" class="lazyload placeholder" data-original="/medias/throttle.gif" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="throttle"></p><h3 class="bamboo-h warning" id="实现一个节流函数">实现一个节流函数</h3><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">throttle</span>(<span class="params">f, ms = <span class="number">100</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> timer = <span class="literal">null</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (timer) <span class="keyword">return</span></span><br><span class="line">   timer = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      f，apply(<span class="built_in">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">      timer = <span class="literal">null</span></span><br><span class="line">    &#125;, ms)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="总结">总结</h2><br/><p><strong>防抖：用户输入结束或暂停时，才会触发change事件</strong></p><p><strong>节流：无论输入速度多快，每隔一定时间只会触发一次</strong></p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 性能优化 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JS 实现继承的几种方式</title>
      <link href="/2021/06/19/js-shi-xian-ji-cheng-de-ji-chong-fang-shi/"/>
      <url>/2021/06/19/js-shi-xian-ji-cheng-de-ji-chong-fang-shi/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>继承</strong>作为面向对象语言的三大特性之一，可以在不影响父类对象实现的情况下，使得子类对象具有父类对象的特性；同时还能再不影响父类对象行为的情况下扩展子类对象独有的特性，为编码带来了极大的便利。<br>下面我们就来看看 <code>JavaScript</code> 中都有哪些实现继承的方法。</p></blockquote><h2 class="bamboo-h " id="原型链继承">原型链继承</h2><br/><p>原型链继承是比较常见的继承方式之一，其中涉及的构造函数、原型和实例，三者之间存在着一定的关系，即<strong>每一个构造函数都有一个原型对象，原型对象又包含一个指向构造函数的指针，而实例则包含一个原型对象的指针。</strong></p><blockquote><p>原型链继承的主要思想是：<strong>重写子类的prototype属性，将其指向父类的实例</strong>。<br>下面我们结合代码来了解一下。</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 属性</span></span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.type = <span class="string">&#x27;Animal&#x27;</span></span><br><span class="line">  <span class="comment">// 实例函数</span></span><br><span class="line">  <span class="built_in">this</span>.sleep = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name + <span class="string">&#x27;正在睡觉&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型函数</span></span><br><span class="line">Animal.prototype.eat = <span class="function"><span class="keyword">function</span> (<span class="params">food</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span>正在吃<span class="subst">$&#123;food&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型继承</span></span><br><span class="line">Cat.prototype = <span class="keyword">new</span> Animal()</span><br><span class="line"><span class="comment">// 将Cat的构造函数指向自身</span></span><br><span class="line">Cat.prototype.constructor = Cat</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cat = <span class="keyword">new</span> Cat(<span class="string">&#x27;Tom&#x27;</span>)</span><br><span class="line"><span class="built_in">console</span>.log(cat.name) <span class="comment">// Tom</span></span><br><span class="line"><span class="built_in">console</span>.log(cat.type) <span class="comment">// Animal</span></span><br><span class="line">cat.sleep() <span class="comment">// Tom正在睡觉</span></span><br><span class="line">cat.eat(<span class="string">&#x27;猫罐头&#x27;</span>) <span class="comment">// Tom正在吃猫罐头</span></span><br></pre></td></tr></table></figure><p>在子类<code>Cat</code>中，我们没有增加<code>type</code>属性，因此会直接继承父类<code>Animal</code>的<code>type</code>属性。</p><p>在子类<code>Cat</code>中，我们增加了<code>name</code>属性，在生成子类实例时，<code>name</code>属性会覆盖父类<code>Animal</code>属性值。</p><p>同样因为<code>Cat</code>的<code>prototype</code>属性指向了<code>Animal</code>类型的实例，因此在生成实例<code>Cat</code>时，会继承实例函数和原型函数。</p><blockquote><p>需要注意：</p><p>Cat.prototype.constructor = Cat</p><p>如果不将Cat原型对象的constructor属性指向自身的构造函数，那将指向父类Animal的构造函数。</p></blockquote><h3 class="bamboo-h warning" id="原型链继承的优点">原型链继承的优点</h3><br/><h4 class="bamboo-h red" id="简单，易于实现">简单，易于实现</h4><br/><p>只需要设置子类的<code>prototype</code>属性指向父类的实例即可。</p><h4 class="bamboo-h red" id="可通过子类直接访问父类原型链属性和函数">可通过子类直接访问父类原型链属性和函数</h4><br/><h3 class="bamboo-h warning" id="原型链继承的缺点">原型链继承的缺点</h3><br/><h4 class="bamboo-h red" id="子类的所有实例将共享父类的属性">子类的所有实例将共享父类的属性</h4><br/><p>子类的所有实例将共享父类的属性会带来一个很严重的问题，父类包含<strong>引用值</strong>时，子类的实例改变该引用值会在所有实例中共享。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.skill = [<span class="string">&#x27;eat&#x27;</span>, <span class="string">&#x27;jump&#x27;</span>, <span class="string">&#x27;sleep&#x27;</span>]</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">Cat.prototype = <span class="keyword">new</span> Animal()</span><br><span class="line">Cat.prototype.constructor = Cat</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cat1 = <span class="keyword">new</span> Cat()</span><br><span class="line"><span class="keyword">let</span> cat2 = <span class="keyword">new</span> Cat()</span><br><span class="line">cat1.skill.push(<span class="string">&#x27;walk&#x27;</span>)</span><br><span class="line"><span class="built_in">console</span>.log(cat1.skill) <span class="comment">// [&quot;eat&quot;, &quot;jump&quot;, &quot;sleep&quot;, &quot;walk&quot;]</span></span><br><span class="line"><span class="built_in">console</span>.log(cat2.skill) <span class="comment">// [&quot;eat&quot;, &quot;jump&quot;, &quot;sleep&quot;, &quot;walk&quot;]</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="在子类实例化时，无法向父类的构造函数传参">在子类实例化时，无法向父类的构造函数传参</h4><br/><p>在通过<code>new</code>操作符创建子类的实例时，会调用子类的构造函数，而在子类的构造函数中并没有设置与父类关联，从而导致无法向父类的构造函数传递参数。</p><h4 class="bamboo-h red" id="无法实现多继承">无法实现多继承</h4><br/><p>子类的<code>prototype</code>只能设置一个值，设置多个值时，后面的值会覆盖前面的值。</p><h2 class="bamboo-h " id="构造函数继承（借助 call）">构造函数继承（借助 call）</h2><br/><blockquote><p>构造函数继承的主要思想：<strong>在子类的构造函数中通过call()函数改变thi的指向，调用父类的构造函数，从而将父类的实例的属性和函数绑定到子类的this上。</strong></p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">  <span class="comment">// 父类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 属性</span></span><br><span class="line">  <span class="built_in">this</span>.name = <span class="string">&#x27;Animal&#x27;</span></span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">  <span class="comment">// 实例函数</span></span><br><span class="line">  <span class="built_in">this</span>.sleep = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name + <span class="string">&#x27;正在睡觉&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型函数</span></span><br><span class="line">Animal.prototype.eat = <span class="function"><span class="keyword">function</span> (<span class="params">food</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span>正在吃<span class="subst">$&#123;food&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 核心，通过call()函数实现Animal的实例的属性和函数的继承</span></span><br><span class="line">  Animal.call(<span class="built_in">this</span>)</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cat = <span class="keyword">new</span> Cat(<span class="string">&#x27;Tom&#x27;</span>)</span><br><span class="line">cat.sleep() <span class="comment">// Tom正在睡觉</span></span><br><span class="line">cat.eat() <span class="comment">// Uncaught TypeError: cat.eat is not a function</span></span><br></pre></td></tr></table></figure><p>通过代码可以发现，子类可以正常调用父类的实例函数，而无法调用父类原型上的函数，<strong>这是因为子类并没有通过某种方式来调用父类原型对象上的函数</strong>。</p><h3 class="bamboo-h warning" id="构造继承的优点">构造继承的优点</h3><br/><h4 class="bamboo-h red" id="解决了子类实例共享父类属性的问题">解决了子类实例共享父类属性的问题</h4><br/><p><code>call()</code>函数实际时改变父类<code>Animal</code>构造函数中<code>this</code>的指向，然后调用<code>this</code>指向了子类<code>Cat</code>，相当于将父类的属性和函数直接绑定到了子类的<code>this</code>中，成了子类实例的熟属性和函数，因此生成的子类实例中是各自拥有自己的属性和函数，不会相互影响。</p><h4 class="bamboo-h red" id="创建子类的实例时，可以向父类传参">创建子类的实例时，可以向父类传参</h4><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">   <span class="comment">// 父类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = <span class="string">&#x27;Animal&#x27;</span></span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params">name, parentAge</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 在子类生成实例时，传递参数给call()函数，间接地传递给父类，然后被子类继承</span></span><br><span class="line">  Animal.call(<span class="built_in">this</span>, parentAge)</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cat = <span class="keyword">new</span> Cat(<span class="string">&#x27;Tom&#x27;</span>, <span class="number">10</span>)</span><br><span class="line"><span class="built_in">console</span>.log(cat.age)</span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="可以实现多继承">可以实现多继承</h4><br/><p>在子类的构造函数中，可以多次调用<code>call()</code>函数来继承多个父对象。</p><h3 class="bamboo-h warning" id="构造函数的缺点">构造函数的缺点</h3><br/><h4 class="bamboo-h red" id="实例只是子类的实例，并不是父类的实例">实例只是子类的实例，并不是父类的实例</h4><br/><p>因为我们并未通过原型对象将子类与父类进行串联，所以生成的实例与父类并没有关系。</p><h4 class="bamboo-h red" id="只能继承父类实例的属性和函数，并不能继承原型对象上的属性和函数">只能继承父类实例的属性和函数，并不能继承原型对象上的属性和函数</h4><br/><p>与上面原因相同。</p><h4 class="bamboo-h red" id="无法复用父类的构造函数">无法复用父类的构造函数</h4><br/><p>因为父类的实例函数将通过<code>call()</code>函数绑定到子类的<code>this</code>中，因此子类生成的每个实例都会拥有父类实例的引用，这会造成不必要的内存消耗，影响性能。</p><h2 class="bamboo-h " id="组合继承">组合继承</h2><br/><blockquote><p>组合继承的主要思想：<strong>结合构造继承和原型继承的两种方式，一方面在子类的构造函数中通过call()函数调用父类的构造函数，将父类的实例的属性和函数绑定到子类的this中；另一方面，通过改变子类的prototype属性，继承父类的原型对象上的属性和函数。</strong></p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 父类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 实例属性</span></span><br><span class="line">  <span class="built_in">this</span>.name = <span class="string">&#x27;Animal&#x27;</span></span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">  <span class="built_in">this</span>.skill = [<span class="string">&#x27;eat&#x27;</span>, <span class="string">&#x27;jump&#x27;</span>, <span class="string">&#x27;sleep&#x27;</span>]</span><br><span class="line">  <span class="comment">// 实例函数</span></span><br><span class="line">  <span class="built_in">this</span>.sleep = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name + <span class="string">&#x27;正在睡觉&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型函数</span></span><br><span class="line">Animal.prototype.eat = <span class="function"><span class="keyword">function</span> (<span class="params">food</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span>正在吃<span class="subst">$&#123;food&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 通过构造函数继承实例的属性和函数</span></span><br><span class="line">  Animal.call(<span class="built_in">this</span>)</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 通过原型继承原型对象上的属性和函数</span></span><br><span class="line">Cat.prototype = <span class="keyword">new</span> Animal()</span><br><span class="line">Cat.prototype.constructor = Cat</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cat = <span class="keyword">new</span> Cat(<span class="string">&#x27;Tom&#x27;</span>)</span><br><span class="line"><span class="built_in">console</span>.log(cat.name) <span class="comment">// Tom</span></span><br><span class="line">cat.sleep() <span class="comment">// Tom正在睡觉</span></span><br><span class="line">cat.eat(<span class="string">&#x27;猫罐头&#x27;</span>) <span class="comment">// Tom正在吃猫罐头</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="组合继承的优点">组合继承的优点</h3><br/><h4 class="bamboo-h red" id="既能继承父类实例的属性和函数，又能继承原型对象上的属性和函数">既能继承父类实例的属性和函数，又能继承原型对象上的属性和函数</h4><br/><h4 class="bamboo-h red" id="既是子类的实例，又是父类的实例">既是子类的实例，又是父类的实例</h4><br/><h4 class="bamboo-h red" id="不存在引用属性共享的问题">不存在引用属性共享的问题</h4><br/><p>构造函数作用域优先级比原型链优先级高，所以不会出现引用属性共享的问题。</p><h4 class="bamboo-h red" id="可以向父类的构造函数中传参">可以向父类的构造函数中传参</h4><br/><h3 class="bamboo-h warning" id="组合继承的缺点">组合继承的缺点</h3><br/><h4 class="bamboo-h red" id="父类的实例属性会被绑定两次">父类的实例属性会被绑定两次</h4><br/><p>在子类的构造函数中，通过<code>call()</code>函数调用了一次父类的构造函数；在改写子类的<code>prototype</code>属性，生成的实例时又调用了一次父类的构造函数。</p><h2 class="bamboo-h " id="寄生组合继承">寄生组合继承</h2><br/><p>组合继承方案已经足够好，但是针对其存在的缺点，我们仍然可以进行优化。</p><p><strong>在进行子类的<code>prototype</code>属性的设置时，可以去掉父类实例的属性的函数</strong>。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">  <span class="comment">//父类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span> (<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 实例属性</span></span><br><span class="line">  <span class="built_in">this</span>.name = <span class="string">&#x27;Animal&#x27;</span></span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">  <span class="built_in">this</span>.skill = [<span class="string">&#x27;eat&#x27;</span>, <span class="string">&#x27;jump&#x27;</span>, <span class="string">&#x27;sleep&#x27;</span>]</span><br><span class="line">  <span class="comment">// 实例函数</span></span><br><span class="line">  <span class="built_in">this</span>.sleep = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name + <span class="string">&#x27;正在睡觉&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型函数</span></span><br><span class="line">Animal.prototype.eat = <span class="function"><span class="keyword">function</span> (<span class="params">food</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span>正在吃<span class="subst">$&#123;food&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 子类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span> (<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 继承父类的实例和属性</span></span><br><span class="line">  Animal.call(<span class="built_in">this</span>)</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 继承父类原型上的实例和属性</span></span><br><span class="line">Cat.prototype = <span class="built_in">Object</span>.create(Animal.prototype)</span><br><span class="line">Cat.prototype.constructor = Cat</span><br><span class="line"><span class="keyword">let</span> cat = <span class="keyword">new</span> Cat(<span class="string">&#x27;Tom&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(cat.name) <span class="comment">// Tom</span></span><br><span class="line">cat.sleep() <span class="comment">// Tom正在睡觉</span></span><br><span class="line">cat.eat(<span class="string">&#x27;猫罐头&#x27;</span>) <span class="comment">// Tom正在吃猫罐头</span></span><br></pre></td></tr></table></figure><blockquote><p>其中最关键的语句：</p><p>Cat.prototype = Object.create(Animal.prototype)</p><p>只取父类Animal的prototype属性，过滤掉Animal的实例属性，从而避免了父类的实例属性绑定两次。</p></blockquote><p>这种寄生组合式继承方式，基本可以解决前几种继承方式的缺点，较好地实现了继承想要的结果，同时也减少了构造次数，减少了性能的开销。</p><p><strong>整体看下来，这六种继承方式中，寄生组合式继承是这里面最优的继承方式。</strong></p><h2 class="bamboo-h " id="总结">总结</h2><br/><p><img "" class="lazyload placeholder" data-original="/medias/extends.png" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="继承"></p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>理解 ES6 的 Map 数据结构</title>
      <link href="/2021/06/14/li-jie-es6-de-map-shu-ju-jie-gou/"/>
      <url>/2021/06/14/li-jie-es6-de-map-shu-ju-jie-gou/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="Map">Map</h2><br/><p>ES6 新增了一种数据结构 Map，与传统的对象字面量类似，它的本质是一种<strong>键值对的组合</strong>。但是与对象字面量不同的是，对象字面量的键只能是<strong>字符串</strong>，对于非字符串类型的值会采用强制类型转换成字符串，而 Map 的键却可以由各种类型的值组成。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 传统的对象类型</span></span><br><span class="line"><span class="keyword">let</span> xx = &#123;<span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span>&#125;</span><br><span class="line"><span class="keyword">let</span> obj = &#123;&#125;</span><br><span class="line">obj[xx] = <span class="string">&#x27;123&#x27;</span> <span class="comment">// &#123;[object Object]: &quot;123&quot;&#125;</span></span><br><span class="line">alert( obj[<span class="string">&quot;[object Object]&quot;</span>] ) <span class="comment">// 123</span></span><br></pre></td></tr></table></figure><p>在上面案例中，采用的是传统的对象处理方式，我们将对一个对象作为对象的键，在输出时会将所有的键如 xx  转换为字符串。所以我们得到字符串键 “[object Object]”。这显然不是我们想要的结果。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> xx = &#123;<span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span>&#125;</span><br><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(xx, <span class="number">123</span>) <span class="comment">// Map(1) &#123;&#123;…&#125; =&gt; 123&#125;</span></span><br><span class="line">alert( map.get(xx) ) <span class="comment">// 123</span></span><br></pre></td></tr></table></figure><p>使用 Map 处理方法，将一个对象作为键添加到 map 实例中，在输出时发现，键的值为对象的真实值，并没有转换成字符串。</p><p>当创建一个 Map 后，我们可以传入一个带有键值对的数组（或其它可迭代对象）来进行初始化。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>([</span><br><span class="line">  [<span class="string">&#x27;name&#x27;</span>,<span class="string">&#x27;xx&#x27;</span>],</span><br><span class="line">  [<span class="string">&#x27;age&#x27;</span>, <span class="number">23</span>]</span><br><span class="line">])</span><br><span class="line"><span class="built_in">console</span>.log(map) <span class="comment">// Map(2) &#123;&quot;name&quot; =&gt; &quot;xx&quot;, &quot;age&quot; =&gt; 23&#125;</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="Map 的属性和方法">Map 的属性和方法</h2><br/><ul><li>new Map() —— 创建 map。</li><li>map.set(key, value) —— 根据键存储值。</li><li>map.get(key) —— 根据键来返回值，如果 map 中不存在对应的 key，则返回 undefined。</li><li>map.has(key) —— 如果 key 存在则返回 true，否则返回 false。</li><li>map.delete(key) —— 删除指定键的值。</li><li>map.clear() —— 清空 map。</li><li>map.size —— 返回当前元素个数。<blockquote><p>map[key] 不是使用 Map 的正确方式</p><p>虽然 map[key] 也有效，例如我们可以设置 map[key] = 23，这样会将 map 视为 JavaScript 的 plainobject，因此它暗含了所有相应的限制（没有对象键等）。<br>所以我们应该使用 map 方法：set 和 get 等。</p></blockquote></li></ul><p>在 Map 数据结构中，所有的键都必须具有<strong>唯一性</strong>。如果对同一个键进行多次赋值，那么后面的值会覆盖前面的值。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(<span class="number">1</span>, <span class="string">&#x27;aa&#x27;</span>)</span><br><span class="line">map.set(<span class="number">1</span>, <span class="string">&#x27;bb&#x27;</span>)</span><br><span class="line">map.get(<span class="number">1</span>) <span class="comment">// &#x27;bb&#x27;</span></span><br></pre></td></tr></table></figure><blockquote><p>Map 是怎么比较键的？</p><p>Map 使用 SameValueZero 算法来比较键是否相等。它和严格等于 === 差不多，但区别是 NaN 被看成是等于 NaN。所以 NaN 也可以被用作键。</p></blockquote><blockquote><p>链式调用</p><p>每一次 map.set 调用都会返回 map 本身，所以我们可以进行“链式”调用：</p><p>map.set(‘name’, ‘xx1’).set(‘age’, 23)</p></blockquote><h2 class="bamboo-h " id="Map 的遍历">Map 的遍历</h2><br/><ul><li>map.keys() —— 遍历并返回所有的键，</li><li>map.values() —— 遍历并返回所有的值，</li><li>map.entries() —— 遍历并返回所有的实体[key, value]，<strong>for..of 在默认情况下使用的就是这个</strong>，</li><li>map.forEach(item, key) —— 第一个参数表示值，第二个参数表示键。</li></ul><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(<span class="string">&#x27;name&#x27;</span>, <span class="string">&#x27;xx&#x27;</span>)</span><br><span class="line">map.set(<span class="string">&#x27;age&#x27;</span>, <span class="number">23</span>)</span><br><span class="line"></span><br><span class="line">map.forEach(<span class="function">(<span class="params">value, key</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value, key); </span><br><span class="line">  <span class="comment">// xx name </span></span><br><span class="line">  <span class="comment">// 23 age</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> key <span class="keyword">of</span> map.keys()) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(key);</span><br><span class="line">  <span class="comment">// name</span></span><br><span class="line">  <span class="comment">// age</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> value <span class="keyword">of</span> map.values()) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">  <span class="comment">// xx</span></span><br><span class="line">  <span class="comment">// 23</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> map.entries()) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(key, value);</span><br><span class="line">    <span class="comment">// name xx</span></span><br><span class="line">    <span class="comment">// age 23</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><blockquote><p>map.entries === map[Symbol.iterator]</p></blockquote><blockquote><p>使用插入顺序</p><p>迭代的顺序与插入值的顺序相同。与普通的 Object 不同，Map 保留了此顺序。</p></blockquote><h2 class="bamboo-h " id="Map 与其他数据类型的转换">Map 与其他数据类型的转换</h2><br/><h3 class="bamboo-h warning" id="数组转换成 Map">数组转换成 Map</h3><br/><p>数组转换成 Map，可以通过 Map 构造函数实现，使用 new 操作符生成 Map 实例。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [[<span class="string">&#x27;name&#x27;</span>, <span class="string">&#x27;xx&#x27;</span>], [<span class="string">&#x27;age&#x27;</span>, <span class="number">23</span>]]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>(arr)</span><br><span class="line"><span class="built_in">console</span>.log(map) <span class="comment">// Map(2) &#123;&quot;name&quot; =&gt; &quot;xx&quot;, &quot;age&quot; =&gt; 23&#125;</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="Map 转换成数组">Map 转换成数组</h3><br/><p>Map 转换成数组，可以通过扩展运算符实现。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(<span class="string">&#x27;name&#x27;</span>, <span class="string">&#x27;xx&#x27;</span>)</span><br><span class="line">map.set(<span class="string">&#x27;age&#x27;</span>, <span class="number">23</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> arr = [...map]</span><br><span class="line"><span class="built_in">console</span>.log(arr) <span class="comment">// (2) [Array(2), Array(2)]</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="对象转换成 Map">对象转换成 Map</h3><br/><p>如果我们想从一个已有的普通对象来创建一个 Map，那么我们可以使用内建方法 Object.entries(obj)，该方法返回对象的键/值对数组，该数组格式完全按照 Map 所需的格式。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">  <span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;xx&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;age&#x27;</span>: <span class="number">23</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>(<span class="built_in">Object</span>.entries(obj))</span><br><span class="line"><span class="built_in">console</span>.log(map) <span class="comment">// Map(2) &#123;&quot;name&quot; =&gt; &quot;xx&quot;, &quot;age&quot; =&gt; 23&#125;</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="Map 转换成对象">Map 转换成对象</h3><br/><p>我们刚刚已经学习了如何使用 Object.entries(obj) 从普通对象创建 Map。</p><p>Object.fromEntries 方法的作用是相反的：给定一个具有 [key, value] 键值对的数组，它会根据给定数组创建一个对象。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(<span class="string">&quot;name&quot;</span>, <span class="string">&#x27;xx&#x27;</span>)</span><br><span class="line">map.set(<span class="string">&quot;age&quot;</span>, <span class="number">23</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> obj = <span class="built_in">Object</span>.fromEntries(map)</span><br><span class="line"><span class="built_in">console</span>.log(obj) <span class="comment">// &#123;name: &quot;xx&quot;, age: 23&#125;</span></span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="Map 转换成 Json">Map 转换成 Json</h3><br/><h4 class="bamboo-h red" id="当 Map 的键名都是字符串">当 Map 的键名都是字符串</h4><br/><p>当 Map 的键名都是字符串时，可以先将 Map 转换成对象，然后调用JSON.stringify()函数。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(<span class="string">&quot;name&quot;</span>, <span class="string">&#x27;xx&#x27;</span>).set(<span class="string">&quot;age&quot;</span>, <span class="number">23</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> json = <span class="built_in">JSON</span>.stringify(<span class="built_in">Object</span>.fromEntries(map))</span><br><span class="line"><span class="built_in">console</span>.log(json) <span class="comment">//&#123;&quot;name&quot;:&quot;xx&quot;,&quot;age&quot;:23&#125;</span></span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="当 Map 的键名有非字符串">当 Map 的键名有非字符串</h4><br/><p>当 Map 的键名有非字符串时，我们可以先将 Map 转换成数组，然后调用JSON.stringify()函数</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>()</span><br><span class="line">map.set(&#123;<span class="attr">fn</span>: <span class="string">&#x27;foo&#x27;</span>&#125;, <span class="string">&#x27;xx&#x27;</span>).set(<span class="literal">true</span>, <span class="string">&#x27;abc&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> json = <span class="built_in">JSON</span>.stringify([...map])</span><br><span class="line"><span class="built_in">console</span>.log(json) <span class="comment">// [[&#123;&quot;fn&quot;:&quot;foo&quot;&#125;,&quot;xx&quot;],[true,&quot;abc&quot;]]</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>在Flexbox Froggy 游戏中搞定flex布局</title>
      <link href="/2021/06/13/zai-flexbox-froggy-you-xi-zhong-gao-ding-flex-bu-ju/"/>
      <url>/2021/06/13/zai-flexbox-froggy-you-xi-zhong-gao-ding-flex-bu-ju/</url>
      
        <content type="html"><![CDATA[<div class="note info"><p>最近发现了一款学习flex布局的游戏,分享给大家 =&gt; <a href="http://flexboxfroggy.com/">Flexbox Froggy</a></p></div><p class='p primary'>一共 24 关</p><h2 class="bamboo-h " id="附上参考答案">附上参考答案</h2><br/><div class="note success"><p>1、justify-content: flex-end</p></div><div class="note success"><p>2、justify-content: center</p></div><div class="note success"><p>3、justify-content: space-around</p></div><div class="note success"><p>4、justify-content: space-between</p></div><div class="note success"><p>5、align-items: flex-end</p></div><div class="note success"><p>6、justify-content: center;</br> &nbsp; &nbsp;&nbsp; align-items: center</p></div><div class="note success"><p>7、justify-content: space-around;</br>&nbsp; &nbsp;&nbsp;&nbsp;align-items: flex-end</p></div><div class="note success"><p>8、flex-direction: row-reverse</p></div><div class="note success"><p>9、flex-direction: column</p></div><div class="note success"><p>10、flex-direction: row-reverse;</br>&nbsp; &nbsp;&nbsp;&nbsp;&nbsp; justify-content: flex-end</p></div><div class="note success"><p>11、flex-direction: column;</br>&nbsp; &nbsp;&nbsp;&nbsp;&nbsp; justify-content: flex-end</p></div><div class="note success"><p>12、flex-direction: column-reverse;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; justify-content: space-between</p></div><div class="note success"><p>13、flex-direction: row-reverse;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; justify-content: center;</br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; align-items: flex-end</p></div><div class="note success"><p>14、order: 1</p></div><div class="note success"><p>15、order: -1</p></div><div class="note success"><p>16、align-self: flex-end</p></div><div class="note success"><p>17、order: 1;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; align-self: flex-end</p></div><div class="note success"><p>18、flex-wrap: wrap;</p></div><div class="note success"><p>19、flex-direction: column;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; flex-wrap: wrap</p></div><div class="note success"><p>20、flex-flow: column wrap</p></div><div class="note success"><p>21、align-content: flex-start</p></div><div class="note success"><p>22、align-content: flex-end</p></div><div class="note success"><p>23、flex-direction: column-reverse;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; align-content: center</p></div><div class="note success"><p>24、flex-flow: column-reverse wrap-reverse;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; justify-content: center;</br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; align-content: space-between</p></div><h2 class="bamboo-h " id="总结">总结</h2><br/><blockquote><p>Flex 布局原理就是：给父盒子添加 flex 属性，来控制子盒子的位置排列方式从而实现 flex 布局。<br>当我们为父盒子设为 flex 布局以后，子元素的 float、clear 和 vertical-align 属性将失效。</p></blockquote><h3 class="bamboo-h warning" id="flex-direction">flex-direction</h3><br/><p>Flex 布局中默认的主轴是 row，x轴。</p><p>如果想改变主轴方向可通过设置 flex-direction 来改变。</p><p>flex-direction:column;将主轴改为y轴，纵轴。</p><p>flex-direction:row; 将主轴改为x轴，横轴。</p><p>flex-direction:row- reverse;主轴为x轴，<strong>并且翻转</strong>。</p><p>flex-direction:column- reverse;主轴为y轴，<strong>并且翻转</strong>。</p><h3 class="bamboo-h warning" id="justify-content">justify-content</h3><br/><p>通过 justify-content 能够设置主轴子元素排列形式。</p><p>值为 flex-start 所有子元素在主轴头部显示。</p><p>值为 flex-end 所有子元素在主轴尾部显示。</p><p>值为 flex-center 所有子元素在主轴居中对齐。</p><p>值为 space-around 所有子元素平分剩余空间。</p><p>值为 space-between 所有子元素先两边贴边在平分剩余空间。</p><h3 class="bamboo-h warning" id="flex-wrap">flex-wrap</h3><br/><p>flex-wrap<br>控制子元素是否换行。</p><p>开启 flex 布局后默认为不换行。</p><p>如果想要换行效果设置 flex-wrap：wrap。</p><h3 class="bamboo-h warning" id="align-items">align-items</h3><br/><p>align-items 设置侧轴子元素排列。</p><p>利用align-items 能够设置侧轴元素对齐的方式在子项为单项（单行）的时候使用。</p><p>align-items 的值为flex-start 表示从头开始。</p><p>align-items 的值为flex-end 表示从结尾开始。</p><p>align-items 的值为center 表示居中显示。</p><p>align-items 的值为stretch 会将子元素拉伸。</p><h3 class="bamboo-h warning" id="align-content">align-content</h3><br/><p>align-content 设置侧轴子元素排列。</p><p> Align-content 适应于换行（多行）的情况下（单行情况下无效），可以设置上对齐、下对齐、居中、拉伸以及平均分配剩余空间等属性值。</p><p> Align-item 和 align-content 的区别单行找 align-items 多行找 align-content。</p><h3 class="bamboo-h warning" id="flex-flow">flex-flow</h3><br/><p> flex-flow 就是 flex-direction 和 flex-wrap 的合写。</p><h3 class="bamboo-h warning" id="order">order</h3><br/><p>order是设置在items里面的属性,具体作用是可以决定本身的排列先后顺序。</p><p>子元素可以通过设置 order 数值的大小来设定在页面中出现的顺序，数值小的在前，数值大的在后。</p><h3 class="bamboo-h warning" id="align-self">align-self</h3><br/><p>align-self 属性定义 flex 子项单独在侧轴（纵轴）方向上的对齐方式，并覆盖已有的 align-items 的值。</p><p>此属性接受与align-items特定项目的值相同的值。</p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> CSS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>用js代码实现观察者模式</title>
      <link href="/2021/06/11/yong-js-dai-ma-shi-xian-guan-cha-zhe-mo-shi/"/>
      <url>/2021/06/11/yong-js-dai-ma-shi-xian-guan-cha-zhe-mo-shi/</url>
      
        <content type="html"><![CDATA[<h1 class="bamboo-h " id="用js代码实现观察者模式">用js代码实现观察者模式</h1><br/><p>观察者模式定义了对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都将得到通知，并自动更新。观察者模式属于三种设计类型(创建型、组合型和行为型)中的行为型模式，行为型模式关注的是对象之间的通讯，观察者模式就是观察者和被观察者之间的通讯。</p><p>总结来说观察者模式有两个特点：</p><p>1.发布&amp;订阅</p><p>2.1对N</p><h1 class="bamboo-h yellow" id="观察者模式的现实举例">观察者模式的现实举例</h1><br/><p>想象一下，你是一位顶尖歌手，粉丝没日没夜地询问你下个单曲什么时候发。 为了从中解放，你承诺(<strong>发布</strong>)会在单曲发布的第一时间发给他们。你给了粉丝们一个列表。他们可以在上面填写他们的电子邮件地址，以便当歌曲发布后，让所有<strong>订阅</strong>了的人能够立即收到。即便遇到不测，例如录音室发生了火灾，以致你无法发布新歌，他们也能及时收到相关通知。</p><h1 class="bamboo-h warning" id="用js代码实现观察者模式">用js代码实现观察者模式</h1><br/><p>首先先画一个 UML 简图</p><p><img "" class="lazyload placeholder" data-original="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/c67be8ca93ce4e6589afbdf147b881a6~tplv-k3u1fbpfcp-watermark.image" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="image-20210611001736865.png"></p><p>根据 UML 简图用js代码实现</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 主题，保存状态，状态变化后触发所有观察者对象</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Subject</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="built_in">this</span>.state = <span class="number">0</span></span><br><span class="line">    <span class="built_in">this</span>.observers = []</span><br><span class="line">  &#125;</span><br><span class="line">  getState () &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.state</span><br><span class="line">  &#125;</span><br><span class="line">  setState (state) &#123;</span><br><span class="line">    <span class="built_in">this</span>.state = state</span><br><span class="line">    <span class="built_in">this</span>.notifyAllObservers()</span><br><span class="line">  &#125;</span><br><span class="line">  notifyAllObservers () &#123;</span><br><span class="line">    <span class="built_in">this</span>.observers.forEach(<span class="function"><span class="params">observer</span> =&gt;</span> &#123;</span><br><span class="line">      observer.update()</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">  attach (observer) &#123;</span><br><span class="line">    <span class="built_in">this</span>.observers.push(observer)</span><br><span class="line">  &#125;</span><br><span class="line">  remove (observer) &#123;</span><br><span class="line">    <span class="built_in">this</span>.observers = <span class="built_in">this</span>.observers.filter(<span class="function"><span class="params">sub</span> =&gt;</span> sub !== observer)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 观察者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Observer</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params">name, subject</span>) &#123;</span><br><span class="line">    <span class="built_in">this</span>.name = name</span><br><span class="line">    <span class="built_in">this</span>.subject = subject</span><br><span class="line">    <span class="built_in">this</span>.subject.attach(<span class="built_in">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  update () &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> update, state: <span class="subst">$&#123;<span class="built_in">this</span>.subject.getState()&#125;</span>`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 测试</span></span><br><span class="line"><span class="keyword">let</span> s = <span class="keyword">new</span> Subject()</span><br><span class="line"><span class="keyword">let</span> o1 = <span class="keyword">new</span> Observer(<span class="string">&#x27;o1&#x27;</span>, s)</span><br><span class="line"><span class="keyword">let</span> o2 = <span class="keyword">new</span> Observer(<span class="string">&#x27;o2&#x27;</span>, s)</span><br><span class="line"><span class="keyword">let</span> o3 = <span class="keyword">new</span> Observer(<span class="string">&#x27;o3&#x27;</span>, s)</span><br><span class="line">s.setState(<span class="number">1</span>)</span><br><span class="line">s.setState(<span class="number">2</span>)</span><br><span class="line">s.setState(<span class="number">3</span>)</span><br><span class="line">s.remove(o1)</span><br><span class="line">s.setState(<span class="number">4</span>)</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h1 class="bamboo-h red" id="设计原则">设计原则</h1><br/><p>主题和观察者分离，不是主动触发而是被动监听，两者解耦</p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>git常见命令总结</title>
      <link href="/2021/06/08/git-chang-jian-ming-ling-zong-jie/"/>
      <url>/2021/06/08/git-chang-jian-ming-ling-zong-jie/</url>
      
        <content type="html"><![CDATA[<h2 id="常见命令"><a href="#常见命令" class="headerlink" title="常见命令"></a>常见命令</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">git clone &lt;url&gt;</span><br><span class="line">git init </span><br><span class="line">git status 查看状态</span><br><span class="line">git diff 查看变更内容</span><br><span class="line">git pull </span><br><span class="line">git push 3 // master 分支</span><br><span class="line">git push origin &lt;branch&gt; // 其他分支</span><br><span class="line">git add . 1</span><br><span class="line">git add &lt;file&gt;</span><br><span class="line">git commit -m &#x27;xxx&#x27; 2</span><br><span class="line">git -commit -m &#x27;xxx -a // 新增所有</span><br><span class="line">git branch &lt;new branch&gt;</span><br><span class="line">git branch &lt;branch&gt; -d // 删除分支</span><br><span class="line">git checkout 切换分支</span><br><span class="line">git merge &lt;branch&gt;</span><br><span class="line">git mv &lt;file&gt; &lt;newfilename&gt; 修改文件名字</span><br><span class="line">git rm &lt;file&gt; // 删除文件</span><br><span class="line">git log // 查看历史</span><br><span class="line">git tag &lt;tagname&gt;</span><br><span class="line">git checkout a.js // 撤销</span><br><span class="line">git revert &lt;commit&gt; // 回滚 撤销指定的提交 有历史记录</span><br><span class="line">git reset --hard &lt;commit&gt; // 撤销指定的提交 无历史记录</span><br><span class="line">=&gt; git push &#x27;xx&#x27; --force</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">(1). 基本使用</span><br><span class="line">git pull 拉取并merge代码</span><br><span class="line">git add .; //添加 暂存区</span><br><span class="line">git commit -m &#x27;zhu shi&#x27;// 提交到本地仓库并加上注释</span><br><span class="line">git push origin master //往远程仓库推送代码</span><br><span class="line">(2). 多人协作方式</span><br><span class="line">git branch -a 查看所有的分支</span><br><span class="line">git checkout -b aaa 创建新的分支aaa</span><br><span class="line">git checkout aaa 切换到aaa分支</span><br><span class="line">git push origin aaa 推送aaa 分支到远程仓库aaa分支</span><br><span class="line">git push origin master:aaa 推送master 到远程的aaa 分支</span><br><span class="line">git branch -d 删除一个分支</span><br></pre></td></tr></table></figure><p><strong>vscode 安装 git history diff</strong></p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 的生命周期</title>
      <link href="/2021/06/07/vue-de-sheng-ming-zhou-qi/"/>
      <url>/2021/06/07/vue-de-sheng-ming-zhou-qi/</url>
      
        <content type="html"><![CDATA[<h1 class="bamboo-h " id="beforeCreate(创建前)">beforeCreate(创建前)</h1><br/><div class="note success"><p>在数据观测和初始化事件还未开始</p></div><h1 class="bamboo-h " id="created(创建后)">created(创建后)</h1><br/><div class="note success"><p>完成数据观测，属性和方法的运算，初始化事件，$el属性还没有显示出来。</p></div><h1 class="bamboo-h " id="beforeMount(载入前)">beforeMount(载入前)</h1><br/><div class="note success"><p>在挂载开始之前被调用，相关的render函数首次被调用。实例已完成以下的配置：编译模板，把data里面的数据和模板生成html。注意此时还没有挂载html到页面上。</p></div><h1 class="bamboo-h " id="mounted（载入后）">mounted（载入后）</h1><br/><div class="note success"><p>在el 被新创建的 vm.$el 替换，并挂载到实例上去之后调用。实例已完成以下的配置：用上面编译好的html内容替换el属性指向的DOM对象。完成模板中的html渲染到html页面中。此过程中进行ajax交互。</p></div><h1 class="bamboo-h " id="beforeUpdate（更新前）">beforeUpdate（更新前）</h1><br/><div class="note success"><p>在数据更新之前调用，发生在虚拟DOM重新渲染和打补丁之前。可以在该钩子中进一步地更改状态，不会触发附加的重渲染过程。</p></div><h1 class="bamboo-h " id="updated（更新后）">updated（更新后）</h1><br/><div class="note success"><p>在由于数据更改导致的虚拟DOM重新渲染和打补丁之后调用。调用时，组件DOM已经更新，所以可以执行依赖于DOM的操作。然而在大多数情况下，应该避免在此期间更改状态，因为这可能会导致更新无限循环。该钩子在服务器端渲染期间不被调用。</p></div><h1 class="bamboo-h " id="beforeDestroy（销毁前）">beforeDestroy（销毁前）</h1><br/><div class="note success"><p>在实例销毁之前调用。实例仍然完全可用。</p></div><h1 class="bamboo-h " id="destroyed（销毁后）">destroyed（销毁后）</h1><br/><div class="note success"><p>在实例销毁之后调用。调用后，所有的事件监听器会被移除，所有的子实例也会被销毁。该钩子在服务器端渲染期间不被调用。</p></div>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>arguments 对象的性质和应用</title>
      <link href="/2021/06/04/arguments-dui-xiang-de-xing-zhi-he-ying-yong/"/>
      <url>/2021/06/04/arguments-dui-xiang-de-xing-zhi-he-ying-yong/</url>
      
        <content type="html"><![CDATA[<h1 class="bamboo-h red" id="arguments 对象的性质">arguments 对象的性质</h1><br/><p>arguments 对象是所有函数都具有的一个内置局部变量，表示的是函数实际接收的参数，是一个<strong>类数组</strong>的结构。</p><p>之所以说 arguments 是类数组是因为它除了具有 length 属性外，不具有数组的其他属性。</p><h2 class="bamboo-h " id="函数外部无法访问">函数外部无法访问</h2><br/><p>arguments对象只能在函数内部使用，无法在函数外部访问到 arguments 对象。同时 arguments 对象存在于函数作用域中，一个函数无法直接访问另一个函数的 arguments。</p><blockquote><p>箭头函数没有 “arguments”</p><p>如果我们在箭头函数中访问 arguments，访问到的 arguments 并不属于箭头函数，而是属于箭头函数外部的“普通”函数。</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> <span class="built_in">arguments</span>); <span class="comment">// undefined</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length); <span class="comment">// 4</span></span><br><span class="line">&#125;</span><br><span class="line">fn (<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>)</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="可通过索引访问">可通过索引访问</h2><br/><p>arguments 对象是一个类数组结构，可以通过索引访问，每一项表示对应传递的实参值，如果该项索引值不存在，则会返回 undefined。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span> (<span class="params">num1, num2</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">0</span>]); <span class="comment">// 1</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">1</span>]); <span class="comment">// 2</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">2</span>]); <span class="comment">// undefined</span></span><br><span class="line">&#125;</span><br><span class="line">fn (<span class="number">1</span>,<span class="number">2</span>)</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="由实参决定">由实参决定</h2><br/><p>arguments 对象的值有实参决定，而不是由定义的形参决定，形参与 arguments 对象占用独立的内存空间。关于arguments 对象与形参之间的关系，可以总结为以下几点。</p><blockquote><p>1、arguments 对象的 length 属性在函数调用的时候就已经确定，不会随着韩式的处理而变化。</p><p>2、指定的形参在传递实参的形况下，arguments 对象与形参值相同，并且可以互相改变。</p><p>3、指定的形参在未传递实参的情况下，arguments 对象对应索引值返回 undefined。</p><p>4、指定的形参在未传递实参的情况下，arguments 对象于形参值不能相互改变。</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span> (<span class="params">a, b, c</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length); <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">arguments</span>[<span class="number">0</span>] = <span class="number">11</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(a); <span class="comment">// 11</span></span><br><span class="line"></span><br><span class="line">  b = <span class="number">12</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">1</span>]); <span class="comment">// 12</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">arguments</span>[<span class="number">2</span>] = <span class="number">3</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(c); <span class="comment">// undefined</span></span><br><span class="line"></span><br><span class="line">  c = <span class="number">13</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">2</span>]); <span class="comment">// 3</span></span><br><span class="line">  </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length); <span class="comment">// 2</span></span><br><span class="line">&#125;</span><br><span class="line">fn (<span class="number">1</span>,<span class="number">2</span>)</span><br></pre></td></tr></table></figure><p>通过上面的例子，很好的总结了 arguments 对象的性质。</p><h2 id="arguments-引发的问题：形参失去意义"><a href="#arguments-引发的问题：形参失去意义" class="headerlink" title="arguments 引发的问题：形参失去意义"></a>arguments 引发的问题：形参失去意义</h2><p>我们看一个例子：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span> (<span class="params">a, b, c</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">arguments</span>[<span class="number">0</span>] = <span class="number">11</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(a); <span class="comment">// 11</span></span><br><span class="line">  <span class="built_in">console</span>.log(b); <span class="comment">// 2</span></span><br><span class="line">&#125;</span><br><span class="line">fn (<span class="number">1</span>,<span class="number">2</span>)</span><br></pre></td></tr></table></figure><blockquote><p><strong>注意：永远不要直接修改 arguments 对象</strong></p><p>如同上面的例子，arguments 对象与函数的形参的关系及其脆弱。修改 arguments 对象需要承担函数的形参失去意义的风险。在严格模式下，函数参数不支持对arguments对象的修改。</p></blockquote><h1 class="bamboo-h red" id="arguments 对象的应用">arguments 对象的应用</h1><br/><p>既然 arguments 有着致命的确定，那么我们该怎么应用它呢？我们来看下面几个应用。</p><h2 class="bamboo-h " id="实参的个数判断">实参的个数判断</h2><br/><p>定义一个函数，明确要求在调用时只能传递3个参数，如果传递的参数个数不等于3，则直接抛出异常。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">a, b, c</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">arguments</span>.length !== <span class="number">3</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">&quot;传递的参数个数不为3&quot;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="任意个数的参数处理">任意个数的参数处理</h2><br/><p>定义一个函数，该函数只会特定处理传递的前几个参数，对于后面的参数不论传递多少个都会统一处理。</p><p>例如：定义一个函数，需求将多个字符串使用分隔符相连，并返回一个结果字符串。此时第一个参数表示分隔符，后面所有的参数表示待相连符。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">joinStr</span> (<span class="params">seperator</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// arguments 是一个类数组，并且不能直接操作 arguments</span></span><br><span class="line">  <span class="comment">// 可以通过call()函数间接调用 slice() 函数返回一个新的数组，操作这个数组</span></span><br><span class="line">  <span class="keyword">let</span> str = <span class="built_in">Array</span>.prototype.slice.call(<span class="built_in">arguments</span>, <span class="number">1</span>)</span><br><span class="line">  <span class="keyword">return</span> str.join(seperator)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">joinStr(<span class="string">&#x27;-&#x27;</span>, <span class="string">&#x27;x&#x27;</span>, <span class="string">&#x27;y&#x27;</span>, <span class="string">&#x27;z&#x27;</span>) <span class="comment">// x-y-z</span></span><br><span class="line">joinStr(<span class="string">&#x27;,&#x27;</span>, <span class="string">&#x27;o&#x27;</span>, <span class="string">&#x27;p&#x27;</span>, <span class="string">&#x27;q&#x27;</span>) <span class="comment">// o,p,q</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="模拟函数重载">模拟函数重载</h2><br/><p>函数重载就是在程序中有命名一样的函数，通过参数的判断实现相同函数的不同调用。</p><p>javascript 不能实现函数重载，因为命名一样的函数，后面的会把前面的覆盖掉。我们可以使用 arguments 来模拟函数重载。</p><p>定义一个函数，实现多值求和。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sum</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> arr = [...arguments]</span><br><span class="line">  <span class="keyword">return</span> arr.reduce(<span class="function">(<span class="params">pre, cur</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> pre + cur</span><br><span class="line">  &#125;, <span class="number">0</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(sum(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)); <span class="comment">// 6</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>深入了解 javascript 中的 new 操作符</title>
      <link href="/2021/06/03/shen-ru-liao-jie-javascript-zhong-de-new-cao-zuo-fu/"/>
      <url>/2021/06/03/shen-ru-liao-jie-javascript-zhong-de-new-cao-zuo-fu/</url>
      
        <content type="html"><![CDATA[<div class="note success"><p>new 操作符在执行过程中会改变 this 的指向，所以我们在了解 new 操作符之前，我们先解释一下 this 的用法。</p></div><h2 class="bamboo-h " id="构造函数中 this 的用法">构造函数中 this 的用法</h2><br/><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">new</span> Person(<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>)); <span class="comment">// Person &#123;name: &#x27;xx&#x27;, age: 18&#125;</span></span><br></pre></td></tr></table></figure><p>输出结果中包含了 name 和 age 的信息。</p><p>事实上，我们并没有通过 return 返回任何值，为什么输出信息中会包含 name 和 age 属性呢？ 其中起作用的就是 this 这个关键字。</p><p>我们通过以下代码输出 this ，看看 this 具体的内容。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>) <span class="comment">// Peoson &#123;&#125;</span></span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> Person (<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>)</span><br></pre></td></tr></table></figure><p>我们发现 this 的实际值为 Person 空对象，后两句话就相当于给 Person 对象添加 name 和 age 属性，结果真的是这样吗？我们改写一下 Person 函数。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>)</span><br><span class="line">  Person.name = name</span><br><span class="line">  Person.age = age</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">new</span> Person (<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>) <span class="comment">// Person &#123;&#125;</span></span><br></pre></td></tr></table></figure><p>输出结果中并未包含 name 和 age 属性。</p><p>因为在Javascript中，如果构造函数没有 return 值，则默认 return this。而上面代码中，this 实际是一个 Person 空对象，name 和 age 属性只是被添加到了临时变量 Person 中，为了能让输出结果包含 name 和 age 属性，我们将临时变量 Person 进行 return 就可以了。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> Person = &#123;&#125;</span><br><span class="line">  Person.name = name</span><br><span class="line">  Person.age = age</span><br><span class="line">  <span class="keyword">return</span> Person</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">new</span> Person (<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>)) <span class="comment">// &#123;name: &quot;xx&quot;, age: 18&#125;</span></span><br></pre></td></tr></table></figure><p>换句话说，new Person 做的就是类似的事情。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// this = &#123;&#125;;（隐式创建）</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 添加属性到 this</span></span><br><span class="line">  <span class="built_in">this</span>.name = name;</span><br><span class="line">  <span class="built_in">this</span>.age = age;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// return this;（隐式返回）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过以上分析，我们了解了构造函数中 this 的用法。</p><h2 class="bamboo-h " id="this 与 new 操作符的关系">this 与 new 操作符的关系</h2><br/><p>通过 new 操作符生成一个Person 对象的实例。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> Person = <span class="keyword">new</span> Person()</span><br></pre></td></tr></table></figure><p>new 操作符做了 3 件事情。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1.</span><span class="keyword">let</span> person = &#123;&#125;</span><br><span class="line"><span class="number">2.</span>person.__proto__ = Person.prototype</span><br><span class="line"><span class="number">3.</span>Person.call(person)</span><br></pre></td></tr></table></figure><p>我们自定义一个类似 new 功能的函数，来具体讲解上上面的 3 行代码。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">New</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> obj = &#123;&#125;</span><br><span class="line">  Person.apply(obj, <span class="built_in">arguments</span>)</span><br><span class="line">  <span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(New(<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>)) <span class="comment">// &#123;name: &quot;xx&quot;, age: 18&#125;</span></span><br></pre></td></tr></table></figure><p>返回的结果中也包含 name 和 age 属性，这就证明了 new 运算符对 this 指向的改变。Person.apply(obj, arguments) 调用后Person对象中的 this 就指向了 obj 对象，这样 obj 对象就具有了 name 和 age 属性。</p><p><strong>但是，不仅关注 new 操作符的函数本身，也要关注它的原型属性。</strong></p><h2 class="bamboo-h " id="prototype 属性与 new 的关系">prototype 属性与 new 的关系</h2><br/><p>我们在 Person 的原型上添加一个 sayHi 函数，然后通过New()返回的对象去调用 sayHi 函数。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Person.prototype.sayHi = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi!&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(New(<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>).sayHi()) <span class="comment">// Uncaught TypeError: New(...).sayHi is not a function</span></span><br></pre></td></tr></table></figure><p> 我们发现执行报错了，New 函数返回的对象并没有调用sayHi方法。</p><p> 这是因为 sayHi 方法是属于 Person 原型的函数，只有 Person 原型链上的对象才能继承 sayHi 方法。那么我们该怎么做呢?</p><p> 这里用到的就是__proto__属性，实例的__proto__属性指向创建实例的函数的原型。设置 obj 对象的__proto__值为 Person 对象的 prototype 属性，那么 obj 对象就继承了 Person 原型上的 sayHi 方法。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span> (<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name</span><br><span class="line">  <span class="built_in">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Person.prototype.sayHi = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi!&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">New</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 另一种写法：let obj = Object.create(Person.prototype)</span></span><br><span class="line">  <span class="keyword">let</span> obj = &#123;&#125;</span><br><span class="line">  obj.__proto__ = Person.prototype</span><br><span class="line">  Person.apply(obj, <span class="built_in">arguments</span>)</span><br><span class="line">  <span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(New(<span class="string">&#x27;xx&#x27;</span>, <span class="number">18</span>).sayHi()) <span class="comment">// Hi</span></span><br></pre></td></tr></table></figure><p> 结果输出 ‘Hi’, 方法调用成功。</p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>v-model 双向数据绑定实现原理</title>
      <link href="/2021/06/01/v-model-shuang-xiang-shu-ju-bang-ding-shi-xian-yuan-li/"/>
      <url>/2021/06/01/v-model-shuang-xiang-shu-ju-bang-ding-shi-xian-yuan-li/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="准备工作">准备工作</h2><br/><p>我们来手动实现一个 v-model 的功能。</p><p>首先定义了一个component组件，里面有一个template。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">  <span class="keyword">const</span> CompOne = &#123;</span><br><span class="line">  <span class="attr">props</span>: [<span class="string">&#x27;value&#x27;</span>],</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">      &lt;input type=&quot;text&quot;&gt;&lt;/input&gt;</span></span><br><span class="line"><span class="string">    &lt;/div&gt;</span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后再定义一个vue对象，里面引用了该组件。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">components</span>: &#123;</span><br><span class="line">    CompOne,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">      &lt;comp-one&gt;&lt;/comp-one&gt;</span></span><br><span class="line"><span class="string">    &lt;/div&gt; </span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">&#125;).$mount(root)</span><br></pre></td></tr></table></figure><p>好了，准备工作完成。</p><h2 class="bamboo-h " id="实现">实现</h2><br/><p><img "" class="lazyload placeholder" data-original="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0cc2c223924940259fa124c63a96c3e4~tplv-k3u1fbpfcp-watermark.image" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="1.gif"></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> CompOne = &#123;</span><br><span class="line">  <span class="attr">props</span>: [<span class="string">&#x27;value&#x27;</span>], <span class="comment">// 接收父组件传递的数据</span></span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">      &lt;input type=&quot;text&quot; @input=&quot;handleInput&quot; :value=&quot;value&quot;&gt;&lt;/input&gt; </span></span><br><span class="line"><span class="string">      // 添加一个input事件</span></span><br><span class="line"><span class="string">      // 接收父组件传递的value</span></span><br><span class="line"><span class="string">    &lt;/div&gt;</span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">  <span class="attr">methods</span>: &#123;</span><br><span class="line">    handleInput (e) &#123;</span><br><span class="line">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;input&#x27;</span>, e.target.value) <span class="comment">// 通过$emit，当 value 变化时通知父组件</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img "" class="lazyload placeholder" data-original="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/af3420cbfa4e4a818b203157ce8e3d7a~tplv-k3u1fbpfcp-watermark.image" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="2.gif"></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">components</span>: &#123;</span><br><span class="line">    CompOne,</span><br><span class="line">  &#125;,</span><br><span class="line">  data () &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      <span class="attr">value</span>: <span class="string">&#x27;vue&#x27;</span> </span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">    // value 为传递给子组件的数据</span></span><br><span class="line"><span class="string">    // @input 接收子组件通过 $emit 传递的参数</span></span><br><span class="line"><span class="string">      &lt;comp-one :value=&quot;value&quot; @input=&quot;value = arguments[0]&quot;&gt;&lt;/comp-one&gt; </span></span><br><span class="line"><span class="string">    &lt;/div&gt; </span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">&#125;).$mount(root)</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="效果">效果</h2><br/><p><img "" class="lazyload placeholder" data-original="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f1428aadc50644b9ad1629983bea1780~tplv-k3u1fbpfcp-watermark.image" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="3.gif"></p><p>大功告成，我们实现了 v-model 的功能！</p><h2 class="bamboo-h " id="拓展">拓展</h2><br/><p> 默认情况下，一个组件上的 v-model 会把 value 用作 prop 且把 input 用作 event。</p><p> 但是像单选框、复选框等类型的输入控件可能会将 value attribute 用于不同的目的。model 选项可以用<br> 来避免这样的冲突。<br> <figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">components</span>: &#123;</span><br><span class="line">    CompOne,</span><br><span class="line">  &#125;,</span><br><span class="line">  data () &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      <span class="attr">value</span>: <span class="string">&#x27;vue&#x27;</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">      &lt;comp-one v-model=&quot;value&quot;&gt;&lt;/comp-one&gt; // 使用v-model 满足语法糖规则：属性必须为value，方法名必须为：input</span></span><br><span class="line"><span class="string">    &lt;/div&gt; </span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">&#125;).$mount(root)</span><br></pre></td></tr></table></figure></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 修改子组件 </span></span><br><span class="line"><span class="keyword">const</span> CompOne = &#123;</span><br><span class="line">  <span class="attr">props</span>: [<span class="string">&#x27;value1&#x27;</span>],</span><br><span class="line">  <span class="attr">model</span>: &#123;</span><br><span class="line">      <span class="attr">prop</span>: <span class="string">&quot;value1&quot;</span>, <span class="comment">// 接收的数据 value =&gt; value1</span></span><br><span class="line">      <span class="attr">event</span>: <span class="string">&quot;change&quot;</span> <span class="comment">// $emit 需要绑定的事件 input =&gt; change</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;div&gt;</span></span><br><span class="line"><span class="string">      &lt;input type=&quot;text&quot; @input=&quot;handleInput&quot; :value=&quot;value1&quot;&gt;&lt;/input&gt;</span></span><br><span class="line"><span class="string">    &lt;/div&gt;</span></span><br><span class="line"><span class="string">  `</span>,</span><br><span class="line">  <span class="attr">methods</span>: &#123;</span><br><span class="line">    handleInput (e) &#123;</span><br><span class="line">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;change&#x27;</span>, e.target.value)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这样就可以让 v-model 变得更灵活，属性和方法名可以自定义。</p><h2 class="bamboo-h " id="总结">总结</h2><br/><p>v-model 时一个语法糖，它做了：</p><ol><li><p>绑定数据value</p></li><li><p>触发输入事件input</p></li><li><p>data 更新触发重新渲染</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>现代JS学习笔记</title>
      <link href="/2021/06/01/xian-dai-js-xue-xi-bi-ji/"/>
      <url>/2021/06/01/xian-dai-js-xue-xi-bi-ji/</url>
      
        <content type="html"><![CDATA[<h2 id="一、JS简介"><a href="#一、JS简介" class="headerlink" title="一、JS简介"></a>一、JS简介</h2><p>JS既可以运行在浏览器中，也可以运行在服务器端，甚至可以在任意搭载了Javascript引擎的设备中执行。</p><p>现代的 JavaScript 是一种“安全的”编程语言。它不提供对内存或 CPU 的底层访问，因为它最初是为浏览器创建的，不需要这些功能。</p><h3 id="1-引擎是如何工作的？"><a href="#1-引擎是如何工作的？" class="headerlink" title="1.引擎是如何工作的？"></a>1.引擎是如何工作的？</h3><p>引擎很复杂，但是基本原理很简单。</p><ol><li>引擎（如果是浏览器，则引擎被嵌入在其中）读取（“解析”）脚本。</li><li>然后，引擎将脚本转化（“编译”）为机器语言。</li><li>然后，机器代码快速地执行。</li></ol><p>引擎会对流程中的每个阶段都进行优化。它甚至可以在编译的脚本运行时监视它，分析流经该脚本的数据，并根据获得的信息进一步优化机器代码。</p><h3 id="2-同源策略"><a href="#2-同源策略" class="headerlink" title="2.同源策略"></a>2.同源策略</h3><p>不同的标签页/窗口之间通常互不了解。有时候，也会有一些联系，例如一个标签页通过 JavaScript 打开的另外一个标签页。但即使在这种情况下，如果两个标签页打开的不是同一个网站（<strong>域名、协议或者端口任一不相同的网站</strong>），它们都不能相互通信。</p><p>这就是所谓的“同源策略”。为了解决“同源策略”问题，两个标签页必须 <strong>都</strong> 包含一些处理这个问题的特定的 JavaScript 代码，并均允许数据交换。本教程会讲到这部分相关的知识。</p><p>这个限制也是为了用户的信息安全。例如，用户打开的 <code>http://anysite.com</code> 网页必须不能访问 <code>http://gmail.com</code>（另外一个标签页打开的网页）也不能从那里窃取信息。</p><h2 id="二、JS基础知识"><a href="#二、JS基础知识" class="headerlink" title="二、JS基础知识"></a>二、JS基础知识</h2><h3 id="1-现代模式-“use-strict”"><a href="#1-现代模式-“use-strict”" class="headerlink" title="1.现代模式 “use strict”"></a>1.现代模式 “use strict”</h3><p>长久以来，JavaScript 不断向前发展且并未带来任何兼容性问题。新的特性被加入，旧的功能也没有改变。</p><p>这么做有利于兼容旧代码，但缺点是 JavaScript 创造者的任何错误或不完善的决定也将永远被保留在 JavaScript 语言中。</p><p>这种情况一直持续到 2009 年 ECMAScript 5 (ES5) 的出现。<strong>ES5</strong> 规范增加了新的语言特性并且修改了一些已经存在的特性。为了保证旧的功能能够使用，大部分的修改是默认不生效的。你需要一个特殊的指令 —— <code>&quot;use strict&quot;</code> 来明确地激活这些特性。</p><p><strong>没有类似于 <code>&quot;no use strict&quot;</code> 这样的指令可以使程序返回默认模式。 没有办法取消 <code>use strict</code></strong></p><p>现代 JavaScript 支持 “classes” 和 “modules” —— 高级语言结构（本教程后续章节会讲到），它们会自动启用 <code>use strict</code>。因此，如果我们使用它们，则无需添加 <code>&quot;use strict&quot;</code> 指令。</p><p><strong>因此，目前我们欢迎将 <code>&quot;use strict&quot;;</code> 写在脚本的顶部。稍后，当你的代码全都写在了 class 和 module 中时，你则可以将 <code>&quot;use strict&quot;;</code> 这行代码省略掉。</strong></p><h3 id="2-变量"><a href="#2-变量" class="headerlink" title="2.变量"></a>2.变量</h3><h4 id="变量命名"><a href="#变量命名" class="headerlink" title="变量命名"></a>变量命名</h4><ol><li>变量名称必须仅包含字母，数字，符号 <code>$</code> 和 <code>_</code>。</li><li>首字符必须非数字。</li></ol><p>如果命名包括多个单词，通常采用驼峰式命名法（<a href="https://en.wikipedia.org/wiki/CamelCase">camelCase</a>）。也就是，单词一个接一个，除了第一个单词，其他的每个单词都以大写字母开头：<code>myVeryLongName</code>。</p><p>有趣的是，美元符号 <code>&#39;$&#39;</code> 和下划线 <code>&#39;_&#39;</code> 也可以用于变量命名。它们是正常的符号，就跟字母一样，没有任何特殊的含义。</p><h3 id="3-数据类型"><a href="#3-数据类型" class="headerlink" title="3.数据类型"></a>3.数据类型</h3><p>在 JavaScript 中有 8 种基本的数据类型（译注：7 种原始类型和 1 种引用类型）。</p><h4 id="数学运算是安全的"><a href="#数学运算是安全的" class="headerlink" title="数学运算是安全的"></a><strong>数学运算是安全的</strong></h4><p>在 JavaScript 中做数学运算是安全的。我们可以做任何事：除以 0，将非数字字符串视为数字，等等。</p><p>脚本永远不会因为一个致命的错误（“死亡”）而停止。最坏的情况下，我们会得到 <code>NaN</code> 的结果。</p><p><strong>在 JavaScript 中，“number” 类型无法表示大于 <code>(253-1)</code>（即 <code>9007199254740991</code>），或小于 <code>-(253-1)</code> 的整数。这是其内部表示形式导致的技术限制。</strong></p><h4 id="String类型"><a href="#String类型" class="headerlink" title="String类型"></a>String类型</h4><p>在 JavaScript 中，有三种包含字符串的方式。</p><ol><li>双引号：<code>&quot;Hello&quot;</code>.</li><li>单引号：<code>&#39;Hello&#39;</code>.</li><li>反引号：<code>Hello</code>.</li></ol><h4 id="null类型"><a href="#null类型" class="headerlink" title="null类型"></a>null类型</h4><p>相比较于其他编程语言，JavaScript 中的 <code>null</code> 不是一个“对不存在的 <code>object</code> 的引用”或者 “null 指针”。</p><p>JavaScript 中的 <code>null</code> 仅仅是一个代表“无”、“空”或“值未知”的特殊值。</p><p>上面的代码表示 <code>age</code> 是未知的。</p><p>JavaScript的最初版本是这样区分的：<strong>null是一个表示”无”的对象，转为数值时为0；undefined是一个表        示”无”的原始值，转为数值时为NaN。</strong></p><p>现在的用法：</p><p><strong>null表示”没有对象”，即该处不应该有值。</strong></p><p>（1） 作为函数的参数，表示该函数的参数不是对象。</p><p>（2） 作为对象原型链的终点</p><p><strong>undefined表示”缺少值”，就是此处应该有一个值，但是还没有定义。</strong></p><h4 id="typeof运算符"><a href="#typeof运算符" class="headerlink" title="typeof运算符"></a>typeof运算符</h4><p><code>typeof</code> 运算符返回参数的类型。当我们想要分别处理不同类型值的时候，或者想快速进行数据类型检验时，非常有用。</p><p>它支持两种语法形式：</p><ol><li>作为运算符：<code>typeof x</code>。</li><li>函数形式：<code>typeof(x)</code>。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">typeof undefined // &quot;undefined&quot;</span><br><span class="line"></span><br><span class="line">typeof 0 // &quot;number&quot;</span><br><span class="line"></span><br><span class="line">typeof 10n // &quot;bigint&quot;</span><br><span class="line"></span><br><span class="line">typeof true // &quot;boolean&quot;</span><br><span class="line"></span><br><span class="line">typeof &quot;foo&quot; // &quot;string&quot;</span><br><span class="line"></span><br><span class="line">typeof Symbol(&quot;id&quot;) // &quot;symbol&quot;</span><br><span class="line"></span><br><span class="line">typeof Math // &quot;object&quot;  (1)</span><br><span class="line"></span><br><span class="line">typeof null // &quot;object&quot;  (2)</span><br><span class="line"></span><br><span class="line">typeof alert // &quot;function&quot;  (3)</span><br></pre></td></tr></table></figure><p><strong><code>null</code> 绝对不是一个 <code>object</code>。<code>null</code> 有自己的类型，它是一个特殊值。</strong></p><p><strong>并非JavaScript中的所有内容都是对象，应该说所有内容都可以充当对象。</strong></p><p>原始类型不是  但是有包装函数，可以有对象的属性和方法</p><h3 id="4-类型转换"><a href="#4-类型转换" class="headerlink" title="4.类型转换"></a>4.类型转换</h3><h4 id="字符串转换"><a href="#字符串转换" class="headerlink" title="字符串转换"></a>字符串转换</h4><p>当我们需要一个字符串形式的值时，就会进行字符串转换。比如，<code>alert(value)</code> 将 <code>value</code> 转换为字符串类型，然后显示这个值。我们也可以显式地调用 <code>String(value)</code> 来将 <code>value</code> 转换为字符串类型：</p><h4 id="数字类型转换"><a href="#数字类型转换" class="headerlink" title="数字类型转换"></a>数字类型转换</h4><p>在算术函数和表达式中，会自动进行 number 类型转换。比如，当把除法 <code>/</code> 用于非 number 类型：</p><p>我们也可以使用 <code>Number(value)</code> 显式地将这个 <code>value</code> 转换为 number 类型。</p><p>当我们从 string 类型源（如文本表单）中读取一个值，但期望输入一个数字时，通常需要进行显式转换。如果该字符串不是一个有效的数字，转换的结果会是 <code>NaN</code>。</p><p>number 类型转换规则：</p><table><thead><tr><th>值</th><th align="center">变成……</th></tr></thead><tbody><tr><td>undefined</td><td align="center">NaN</td></tr><tr><td>null</td><td align="center">0</td></tr><tr><td>true/false</td><td align="center">1/0</td></tr><tr><td>string</td><td align="center">去掉首尾空格后的纯数字字符串中含有的数字。如果剩余字符串为空，则转换结果为 <code>0</code>。否则，将会从剩余字符串中“读取”数字。当类型转换出现 error 时返回 <code>NaN</code>。</td></tr></tbody></table><h4 id="布尔型转换"><a href="#布尔型转换" class="headerlink" title="布尔型转换"></a>布尔型转换</h4><p>转换规则如下：</p><ul><li>直观上为“空”的值（如 <code>0</code>、空字符串、<code>null</code>、<code>undefined</code> 和 <code>NaN</code>）将变为 <code>false</code>。</li><li>其他值变成 <code>true</code>。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">&quot;&quot; + 1 + 0 // &#x27;10&#x27;</span><br><span class="line">&quot;&quot; - 1 + 0 // -1</span><br><span class="line">true + false // 1</span><br><span class="line">6 / &quot;3&quot;  // 2</span><br><span class="line">&quot;2&quot; * &quot;3&quot;  // 6</span><br><span class="line">4 + 5 + &quot;px&quot; // &#x27;9px&#x27;</span><br><span class="line">&quot;$&quot; + 4 + 5  // &#x27;$45&#x27;</span><br><span class="line">&quot;4&quot; - 2  // 2</span><br><span class="line">&quot;4px&quot; - 2 // NaN</span><br><span class="line">7 / 0 // infinity</span><br><span class="line">&quot;  -9  &quot; + 5 // &#x27;-9 5&#x27;</span><br><span class="line">&quot;  -9  &quot; - 5 // -14</span><br><span class="line">null + 1 // 1</span><br><span class="line">undefined + 1  // NaN</span><br><span class="line">&quot; \t \n&quot; - 2 // -2</span><br></pre></td></tr></table></figure><h3 id="5-值的比较"><a href="#5-值的比较" class="headerlink" title="5.值的比较"></a>5.值的比较</h3><p>普通的相等性检查 <code>==</code> 存在一个问题，它不能区分出 <code>0</code> 和 <code>false</code>，也同样无法区分空字符串和 <code>false</code></p><p><strong>严格相等运算符 <code>===</code> 在进行比较时不会做任何的类型转换。</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">alert( null &gt; 0 );  // (1) false</span><br><span class="line">alert( null == 0 ); // (2) false</span><br><span class="line">alert( null &gt;= 0 ); // (3) true</span><br></pre></td></tr></table></figure><p>为什么会出现这种反常结果，这是因为相等性检查 <code>==</code> 和普通比较符 <code>&gt; &lt; &gt;= &lt;=</code> 的代码逻辑是相互独立的。进行值的比较时，<code>null</code> 会被转化为数字，因此它被转化为了 <code>0</code>。这就是为什么（3）中 <code>null &gt;= 0</code> 返回值是 true，（1）中 <code>null &gt; 0</code> 返回值是 false。</p><p>另一方面，<code>undefined</code> 和 <code>null</code> 在相等性检查 <code>==</code> 中不会进行任何的类型转换，它们有自己独立的比较规则，所以除了它们之间互等外，不会等于任何其他的值。这就解释了为什么（2）中 <code>null == 0</code> 会返回 false。</p><p><strong>除非你非常清楚自己在做什么，否则永远不要使用 <code>&gt;= &gt; &lt; &lt;=</code> 去比较一个可能为 <code>null/undefined</code> 的变量。对于取值可能是 <code>null/undefined</code> 的变量，请按需要分别检查它的取值情况。</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">5 &gt; 4  // true</span><br><span class="line">&quot;apple&quot; &gt; &quot;pineapple&quot; // false</span><br><span class="line">&quot;2&quot; &gt; &quot;12&quot; // true</span><br><span class="line">undefined == null // true</span><br><span class="line">undefined === null // false</span><br><span class="line">null == &quot;\n0\n&quot; // false</span><br><span class="line">null === +&quot;\n0\n&quot; // false</span><br></pre></td></tr></table></figure><h3 id="6-空值合并运算符"><a href="#6-空值合并运算符" class="headerlink" title="6.空值合并运算符"></a>6.空值合并运算符</h3><p>如果第一个参数不是 <code>null/undefined</code>，则 <code>??</code> 返回第一个参数。否则，返回第二个参数。空值合并运算符并不是什么全新的东西。它只是一种获得两者中的第一个“已定义的”值的不错的语法。</p><h4 id="与-比较"><a href="#与-比较" class="headerlink" title="与||比较"></a>与||比较</h4><p>它们之间重要的区别是：</p><ul><li><code>||</code> 返回第一个 <strong>真</strong> 值。</li><li><code>??</code> 返回第一个 <strong>已定义的</strong> 值。</li></ul><p>换句话说，<code>||</code> 无法区分 <code>false</code>、<code>0</code>、空字符串 <code>&quot;&quot;</code> 和 <code>null/undefined</code>。它们都一样 —— 假值（falsy values）。如果其中任何一个是 <code>||</code> 的第一个参数，那么我们将得到第二个参数作为结果。</p><h4 id="优先级"><a href="#优先级" class="headerlink" title="优先级"></a>优先级</h4><p><code>??</code> 运算符的优先级相当低：在 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence#Table">MDN table</a> 中为 <code>5</code>。因此，<code>??</code> 在 <code>=</code> 和 <code>?</code> 之前计算，但在大多数其他运算符（例如，<code>+</code> 和 <code>*</code>）之后计算。</p><p>因此，如果我们需要在还有其他运算符的表达式中使用 <code>??</code> 进行取值，需要考虑加括号</p><h3 id="7-函数"><a href="#7-函数" class="headerlink" title="7.函数"></a>7.函数</h3><h4 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h4><p>如果未提供参数，那么其默认值则是 <code>undefined</code>。</p><p>如果我们想在本示例中设定“默认”的 <code>text</code>，那么我们可以在 <code>=</code> 之后指定它：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">function showMessage(from, text = &quot;no text given&quot;) &#123;</span><br><span class="line">  alert( from + &quot;: &quot; + text );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">showMessage(&quot;Ann&quot;); // Ann: no text given</span><br></pre></td></tr></table></figure><p>现在如果 <code>text</code> 参数未被传递，它将会得到值 <code>&quot;no text given&quot;</code>。</p><p>在 JavaScript 中，函数是一个<strong>值</strong>，所以我们可以把它当成值对待。上面代码显示了一段字符串值，即函数的源码。</p><p>的确，在某种意义上说一个函数是一个特殊值，我们可以像 <code>sayHi()</code> 这样调用它。</p><p>但它依然是一个值，所以我们可以像使用其他类型的值一样使用它。</p><h4 id="为什么会有分号？"><a href="#为什么会有分号？" class="headerlink" title="为什么会有分号？"></a>为什么会有分号？</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">function sayHi() &#123;</span><br><span class="line">  // ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">let sayHi = function() &#123;</span><br><span class="line">  // ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>答案很简单：</p><ul><li>在代码块的结尾不需要加分号 <code>;</code>，像 <code>if &#123; ... &#125;</code>，<code>for &#123; &#125;</code>，<code>function f &#123; &#125;</code> 等语法结构后面都不用加。</li><li>函数表达式是在语句内部的：<code>let sayHi = ...;</code>，作为一个值。它不是代码块而是一个赋值语句。不管值是什么，都建议在语句末尾添加分号 <code>;</code>。所以这里的分号与函数表达式本身没有任何关系，它只是用于终止语句。</li></ul><h4 id="一个函数是表示一个“行为”的值"><a href="#一个函数是表示一个“行为”的值" class="headerlink" title="一个函数是表示一个“行为”的值"></a><strong>一个函数是表示一个“行为”的值</strong></h4><p>字符串或数字等常规值代表 <strong>数据</strong>。</p><p>函数可以被视为一个 <strong>行为（action）</strong>。</p><p>我们可以在变量之间传递它们，并在需要时运行。</p><p><strong>在编程语言中，一等公民可以作为函数参数，可以作为函数返回值，也可以赋值给变量。</strong></p><h2 id="三、对象"><a href="#三、对象" class="headerlink" title="三、对象"></a>三、对象</h2><h3 id="1-计算属性"><a href="#1-计算属性" class="headerlink" title="1.计算属性"></a>1.计算属性</h3><p>当创建一个对象时，我们可以在对象字面量中使用方括号。这叫做 <strong>计算属性</strong>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">let fruit = prompt(&quot;Which fruit to buy?&quot;, &quot;apple&quot;);</span><br><span class="line"></span><br><span class="line">let bag = &#123;</span><br><span class="line">  [fruit]: 5, // 属性名是从 fruit 变量中得到的</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">alert( bag.apple ); // 5 如果 fruit=&quot;apple&quot;</span><br></pre></td></tr></table></figure><p>这里有个小陷阱：一个名为 <code>__proto__</code> 的属性。我们不能将它设置为一个非对象的值：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">let obj = &#123;&#125;;</span><br><span class="line">obj.__proto__ = 5; // 分配一个数字</span><br><span class="line">alert(obj.__proto__); // [object Object] — 值为对象，与预期结果不同</span><br></pre></td></tr></table></figure><p>我们从代码中可以看出来，把它赋值为 <code>5</code> 的操作被忽略了。</p><h3 id="2-属性存在性测试，‘in’操作"><a href="#2-属性存在性测试，‘in’操作" class="headerlink" title="2.属性存在性测试，‘in’操作"></a>2.属性存在性测试，‘in’操作</h3><p>相对于其他语言，javascript的对象中有一个需要特别注意的特性：能够被访问任何属性。即使属性不存在也不会报错！读取不存在的属性只会得到 <code>undefined</code>。所以我们可以很容易地判断一个属性是否存在</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">let user = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">alert( user.noSuchProperty === undefined ); // true 意思是没有这个属性</span><br></pre></td></tr></table></figure><p>这里还有一个特别的，检查属性是否存在的操作符 <code>&quot;in&quot;</code>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&quot;key&quot; in object</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">let user = &#123; name: &quot;John&quot;, age: 30 &#125;;</span><br><span class="line"></span><br><span class="line">alert( &quot;age&quot; in user ); // true，user.age 存在</span><br><span class="line">alert( &quot;blabla&quot; in user ); // false，user.blabla 不存在。</span><br></pre></td></tr></table></figure><p>请注意，<code>in</code> 的左边必须是 <strong>属性名</strong>。通常是一个带引号的字符串。</p><h4 id="in-与-undefined比较"><a href="#in-与-undefined比较" class="headerlink" title="in 与 undefined比较"></a>in 与 undefined比较</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">let obj = &#123;</span><br><span class="line">  test: undefined</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">alert( obj.test ); // 显示 undefined，所以属性不存在？</span><br><span class="line"></span><br><span class="line">alert( &quot;test&quot; in obj ); // true，属性存在！</span><br></pre></td></tr></table></figure><h4 id="对象属性排序"><a href="#对象属性排序" class="headerlink" title="对象属性排序"></a>对象属性排序</h4><p>对象有顺序吗？换句话说，如果我们遍历一个对象，我们获取属性的顺序是和属性添加时的顺序相同吗？这靠谱吗？</p><p>简短的回答是：“有特别的顺序”：整数属性会被进行排序，其他属性则按照创建的顺序显示。</p><p>我们可以使用非整数属性名来 <strong>欺骗</strong> 程序。只需要给每个键名加一个加号 <code>&quot;+&quot;</code> 前缀就行了。</p><h3 id="3-对象的引用和复制"><a href="#3-对象的引用和复制" class="headerlink" title="3.对象的引用和复制"></a>3.对象的引用和复制</h3><p>与原始类型相比，对象的根本区别之一是对象是“通过引用”被存储和复制的，与原始类型值相反：字符串，数字，布尔值等 —— 始终是以“整体值”的形式被复制的。</p><h3 id="4-垃圾回收"><a href="#4-垃圾回收" class="headerlink" title="4.垃圾回收"></a>4.垃圾回收</h3><p>JavaScript 中主要的内存管理概念是 <strong>可达性</strong>。</p><p>简而言之，“可达”值是那些以某种方式可访问或可用的值。它们一定是存储在内存中的。</p><ol><li><p>这里列出固有的可达值的基本集合，这些值明显不能被释放。</p><p>比方说：</p><ul><li>当前函数的局部变量和参数。</li><li>嵌套调用时，当前调用链上所有函数的变量与参数。</li><li>全局变量。</li><li>（还有一些内部的）</li></ul><p>这些值被称作 <strong>根（roots）</strong>。</p></li><li><p>如果一个值可以通过引用或引用链从根访问任何其他值，则认为该值是可达的。</p></li></ol><p><strong>主要需要掌握的内容：</strong></p><ul><li>垃圾回收是自动完成的，我们不能强制执行或是阻止执行。</li><li>当对象是可达状态时，它一定是存在于内存中的。</li><li>被引用与可访问（从一个根）不同：一组相互连接的对象可能整体都不可达。</li></ul><h3 id="5-this"><a href="#5-this" class="headerlink" title="5.this"></a>5.this</h3><p>在 JavaScript 中，<code>this</code> 是“自由”的，它的值是在调用时计算出来的，它的值并不取决于方法声明的位置，而是取决于在“点符号前”的是什么对象。</p><p>以“方法”的语法调用函数时：<code>object.method()</code>，调用过程中的 <code>this</code> 值是 <code>object</code>。</p><h3 id="6-构造器和操作符‘new’"><a href="#6-构造器和操作符‘new’" class="headerlink" title="6.构造器和操作符‘new’"></a>6.构造器和操作符‘new’</h3><h4 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h4><p>当一个函数被使用 <code>new</code> 操作符执行时，它按照以下步骤：</p><ol><li>一个新的空对象被创建并分配给 <code>this</code>。这个新对象会被执行[[prototype]]连接</li><li>函数体执行。通常它会修改 <code>this</code>，为其添加新的属性。</li><li>返回 <code>this</code> 的值。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">function User(name) &#123;</span><br><span class="line">  // this = &#123;&#125;;（隐式创建）</span><br><span class="line"></span><br><span class="line">  // 添加属性到 this</span><br><span class="line">  this.name = name;</span><br><span class="line">  this.isAdmin = false;</span><br><span class="line"></span><br><span class="line">  // return this;（隐式返回）</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>这是构造器的主要目的 —— 实现可重用的对象创建代码。</strong></p><h4 id="构造器的return"><a href="#构造器的return" class="headerlink" title="构造器的return"></a>构造器的return</h4><p>通常，构造器没有 <code>return</code> 语句。它们的任务是将所有必要的东西写入 <code>this</code>，并自动转换为结果。</p><p>但是，如果这有一个 <code>return</code> 语句，那么规则就简单了：</p><ul><li><strong>如果 <code>return</code> 返回的是一个对象，则返回这个对象，而不是 <code>this</code>。</strong></li><li>如果 <code>return</code> 返回的是一个原始类型，则忽略。</li></ul><p>换句话说，带有对象的 <code>return</code> 返回该对象，在所有其他情况下返回 <code>this</code>。</p><p>例如，这里 <code>return</code> 通过返回一个对象覆盖 <code>this</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">function BigUser() &#123;</span><br><span class="line"></span><br><span class="line">  this.name = &quot;John&quot;;</span><br><span class="line"></span><br><span class="line">  return &#123; name: &quot;Godzilla&quot; &#125;;  // &lt;-- 返回这个对象</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">alert( new BigUser().name );  // Godzilla，得到了那个对象</span><br></pre></td></tr></table></figure><h3 id="7-可选链"><a href="#7-可选链" class="headerlink" title="7.可选链?."></a>7.可选链?.</h3><h4 id="‘不存在属性’的问题"><a href="#‘不存在属性’的问题" class="headerlink" title="‘不存在属性’的问题"></a>‘不存在属性’的问题</h4><p>我们大多数用户的地址都存储在 <code>user.address</code> 中，街道地址存储在 <code>user.address.street</code> 中，但有些用户没有提供这些信息。</p><p>在这种情况下，当我们尝试获取 <code>user.address.street</code>，而该用户恰好没提供地址信息，我们则会收到一个错误：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">let user = &#123;&#125;; // 一个没有 &quot;address&quot; 属性的 user 对象</span><br><span class="line"></span><br><span class="line">alert(user.address.street); // Error!</span><br></pre></td></tr></table></figure><h4 id="可选链"><a href="#可选链" class="headerlink" title="可选链"></a>可选链</h4><p><strong>为了简明起见，在本文接下来的内容中，我们会说如果一个属性既不是 <code>null</code> 也不是 <code>undefined</code>，那么它就“存在”。</strong></p><p>换句话说，例如 <code>value?.prop</code>：</p><ul><li>如果 <code>value</code> 存在，则结果与 <code>value.prop</code> 相同，</li><li>否则（当 <code>value</code> 为 <code>undefined/null</code> 时）则返回 <code>undefined</code>。</li></ul><p>下面这是一种使用 <code>?.</code> 安全地访问 <code>user.address.street</code> 的方式：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">let user = &#123;&#125;; // user 没有 address 属性</span><br><span class="line"></span><br><span class="line">alert( user?.address?.street ); // undefined（不报错）</span><br></pre></td></tr></table></figure><p><strong>我们可以使用</strong> <code>?.</code> <strong>来安全地读取或删除，但不能写入</strong></p><h3 id="8-Sysmbol类型"><a href="#8-Sysmbol类型" class="headerlink" title="8.Sysmbol类型"></a>8.Sysmbol类型</h3><p>根据规范，对象的属性键只能是字符串类型或者 Symbol 类型。不是 Number，也不是 Boolean，只有字符串或 Symbol 这两种类型。</p><h4 id="Symbol"><a href="#Symbol" class="headerlink" title="Symbol"></a>Symbol</h4><p>‘symbol’值表示唯一的标识符</p><p>可以使用symbol() 创建这种类型的值</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">// id 是 symbol 的一个实例化对象</span><br><span class="line">let id = Symbol();</span><br></pre></td></tr></table></figure><p>创建时，我们可以给 Symbol 一个描述（也称为 Symbol 名），这在代码调试时非常有用：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">// id 是描述为 &quot;id&quot; 的 Symbol</span><br><span class="line">let id = Symbol(&quot;id&quot;);</span><br></pre></td></tr></table></figure><p>Symbol 保证是唯一的。即使我们创建了许多具有相同描述的 Symbol，它们的值也是不同。描述只是一个标签，不影响任何东西。</p><p>例如，这里有两个描述相同的 Symbol —— 它们不相等：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">let id1 = Symbol(&quot;id&quot;);</span><br><span class="line">let id2 = Symbol(&quot;id&quot;);</span><br><span class="line"></span><br><span class="line">alert(id1 == id2); // false</span><br></pre></td></tr></table></figure><h4 id="Symbol不会被自动转换为字符串"><a href="#Symbol不会被自动转换为字符串" class="headerlink" title="Symbol不会被自动转换为字符串"></a>Symbol不会被自动转换为字符串</h4><p>JavaScript 中的大多数值都支持字符串的隐式转换。例如，我们可以 <code>alert</code> 任何值，都可以生效。Symbol 比较特殊，它不会被自动转换。</p><p>例如，这个 <code>alert</code> 将会提示出错：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">let id = Symbol(&quot;id&quot;);</span><br><span class="line">alert(id); // 类型错误：无法将 Symbol 值转换为字符串。</span><br></pre></td></tr></table></figure><p>如果我们真的想显示一个 Symbol，我们需要在它上面调用 <code>.toString()</code>，如下所示：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">let id = Symbol(&quot;id&quot;);</span><br><span class="line">alert(id.toString()); // Symbol(id)，现在它有效了</span><br></pre></td></tr></table></figure><p>或者获取 <code>symbol.description</code> 属性，只显示描述（description）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">let id = Symbol(&quot;id&quot;);</span><br><span class="line">alert(id.description); // id</span><br></pre></td></tr></table></figure><h4 id="隐藏属性"><a href="#隐藏属性" class="headerlink" title="隐藏属性"></a>隐藏属性</h4><p>Symbol 允许我们创建对象的“隐藏”属性，代码的任何其他部分都不能意外访问或重写这些属性。</p><p>例如，如果我们使用的是属于第三方代码的 <code>user</code> 对象，我们想要给它们添加一些标识符。</p><p>我们可以给它们使用 Symbol 键：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123; <span class="comment">// 属于另一个代码</span></span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;John&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> id = <span class="built_in">Symbol</span>(<span class="string">&quot;id&quot;</span>);</span><br><span class="line"></span><br><span class="line">user[id] = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">alert( user[id] ); <span class="comment">// 我们可以使用 Symbol 作为键来访问数据</span></span><br></pre></td></tr></table></figure><p><a href="https://developer.mozilla.org/zh/docs/Web/JavaScript/Reference/Global_Objects/Object/assign">Object.assign</a> 会同时复制字符串和 symbol 属性</p><p><code>Object.keys(user)</code> 也会忽略它们。这是一般“隐藏符号属性”原则的一部分。如果另一个脚本或库遍历我们的对象，它不会意外地访问到符号属性。</p><h4 id="全局Symbol"><a href="#全局Symbol" class="headerlink" title="全局Symbol"></a>全局Symbol</h4><p>正如我们所看到的，通常所有的 Symbol 都是不同的，即使它们有相同的名字。但有时我们想要名字相同的 Symbol 具有相同的实体。例如，应用程序的不同部分想要访问的 Symbol <code>&quot;id&quot;</code> 指的是完全相同的属性。</p><p>为了实现这一点，这里有一个 <strong>全局 Symbol 注册表</strong>。我们可以在其中创建 Symbol 并在稍后访问它们，它可以确保每次访问相同名字的 Symbol 时，返回的都是相同的 Symbol。</p><p>要从注册表中读取（不存在则创建）Symbol，请使用 <code>Symbol.for(key)</code>。</p><p>该调用会检查全局注册表，如果有一个描述为 <code>key</code> 的 Symbol，则返回该 Symbol，否则将创建一个新 Symbol（<code>Symbol(key)</code>），并通过给定的 <code>key</code> 将其存储在注册表中。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">// 从全局注册表中读取</span><br><span class="line">let id = Symbol.for(&quot;id&quot;); // 如果该 Symbol 不存在，则创建它</span><br><span class="line"></span><br><span class="line">// 再次读取（可能是在代码中的另一个位置）</span><br><span class="line">let idAgain = Symbol.for(&quot;id&quot;);</span><br><span class="line"></span><br><span class="line">// 相同的 Symbol</span><br><span class="line">alert( id === idAgain ); // true</span><br></pre></td></tr></table></figure><h4 id="Symbol-keyFor"><a href="#Symbol-keyFor" class="headerlink" title="Symbol.keyFor"></a>Symbol.keyFor</h4><p>对于全局 Symbol，不仅有 <code>Symbol.for(key)</code> 按名字返回一个 Symbol，还有一个反向调用：<code>Symbol.keyFor(sym)</code>，它的作用完全反过来：通过全局 Symbol 返回一个名字。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">// 通过 name 获取 Symbol</span><br><span class="line">let sym = Symbol.for(&quot;name&quot;);</span><br><span class="line">let sym2 = Symbol.for(&quot;id&quot;);</span><br><span class="line"></span><br><span class="line">// 通过 Symbol 获取 name</span><br><span class="line">alert( Symbol.keyFor(sym) ); // name</span><br><span class="line">alert( Symbol.keyFor(sym2) ); // id</span><br></pre></td></tr></table></figure><p><code>Symbol.keyFor</code> 内部使用全局 Symbol 注册表来查找 Symbol 的键。所以它不适用于非全局 Symbol。如果 Symbol 不是全局的，它将无法找到它并返回 <code>undefined</code>。</p><p><strong>也就是说，任何 Symbol 都具有 <code>description</code> 属性。</strong></p><h2 id="四、数据类型"><a href="#四、数据类型" class="headerlink" title="四、数据类型"></a>四、数据类型</h2><p>函数 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toFixed">toFixed(n)</a> 将数字舍入到小数点后 <code>n</code> 位，并以<strong>字符串</strong>形式返回结果。</p><p>有一个特殊的内建方法 <a href="https://developer.mozilla.org/zh/docs/Web/JavaScript/Reference/Global_Objects/Object/is"><code>Object.is</code></a>，它类似于 <code>===</code> 一样对值进行比较，但它对于两种边缘情况更可靠：</p><ol><li>它适用于 <code>NaN</code>：<code>Object.is（NaN，NaN）=== true</code>，这是件好事。</li><li>值 <code>0</code> 和 <code>-0</code> 是不同的：<code>Object.is（0，-0）=== false</code>，从技术上讲这是对的，因为在内部，数字的符号位可能会不同，即使其他所有位均为零。</li></ol><p>在所有其他情况下，<code>Object.is(a，b)</code> 与 <code>a === b</code> 相同。</p><p>这种比较方式经常被用在 JavaScript 规范中。当内部算法需要比较两个值是否完全相同时，它使用 <code>Object.is</code>（内部称为 <a href="https://tc39.github.io/ecma262/#sec-samevalue">SameValue</a>）。</p><p><strong>结论：在处理小数时避免相等性检查。</strong></p><p>在 <code>if</code> 测试中 <code>indexOf</code> 有一点不方便。我们不能像这样把它放在 <code>if</code> 中：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">let str = &quot;Widget with id&quot;;</span><br><span class="line"></span><br><span class="line">if (str.indexOf(&quot;Widget&quot;)) &#123;</span><br><span class="line">    alert(&quot;We found it&quot;); // 不工作！</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述示例中的 <code>alert</code> 不会显示，因为 <code>str.indexOf(&quot;Widget&quot;)</code> 返回 <code>0</code>（意思是它在起始位置就查找到了匹配项）。是的，但是 <code>if</code> 认为 <code>0</code> 表示 <code>false</code>。</p><p>因此我们应该检查 <code>-1</code>，像这样：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">let str = &quot;Widget with id&quot;;</span><br><span class="line"></span><br><span class="line">if (str.indexOf(&quot;Widget&quot;) != -1) &#123;</span><br><span class="line">    alert(&quot;We found it&quot;); // 现在工作了！</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th align="left">方法</th><th align="left">选择方式……</th><th align="left">负值参数</th></tr></thead><tbody><tr><td align="left"><code>slice(start, end)</code></td><td align="left">从 <code>start</code> 到 <code>end</code>（不含 <code>end</code>）</td><td align="left">允许</td></tr><tr><td align="left"><code>substring(start, end)</code></td><td align="left"><code>start</code> 与 <code>end</code> 之间（包括 <code>start</code>，但不包括 <code>end</code>）</td><td align="left">负值代表 <code>0</code></td></tr><tr><td align="left"><code>substr(start, length)</code></td><td align="left">从 <code>start</code> 开始获取长为 <code>length</code> 的字符串</td><td align="left">允许 <code>start</code> 为负数</td></tr></tbody></table><p>写一个函数 <code>ucFirst(str)</code>，并返回首字母大写的字符串 <code>str</code>，例如：</p><p><strong>这里存在一个小问题。如果 <code>str</code> 是空的，那么 <code>str[0]</code> 就是 <code>undefined</code>，但由于 <code>undefined</code> 并没有 <code>toUpperCase()</code> 方法，因此我们会得到一个错误。</strong></p><ol><li>使用 <code>str.charAt(0)</code>，因为它总是会返回一个字符串（可能为空）。</li><li>为空字符添加测试。</li></ol><h3 id="1-数组"><a href="#1-数组" class="headerlink" title="1.数组"></a>1.数组</h3><h4 id="splice"><a href="#splice" class="headerlink" title="splice"></a>splice</h4><p>数组是对象，所以我们可以尝试使用 <code>delete</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">let arr = [&quot;I&quot;, &quot;go&quot;, &quot;home&quot;];</span><br><span class="line"></span><br><span class="line">delete arr[1]; // remove &quot;go&quot;</span><br><span class="line"></span><br><span class="line">alert( arr[1] ); // undefined</span><br><span class="line"></span><br><span class="line">// now arr = [&quot;I&quot;,  , &quot;home&quot;];</span><br><span class="line">alert( arr.length ); // 3</span><br></pre></td></tr></table></figure><p>元素被删除了，但数组仍然有 3 个元素，我们可以看到 <code>arr.length == 3</code>。</p><p>这很正常，因为 <code>delete obj.key</code> 是通过 <code>key</code> 来移除对应的值。对于对象来说是可以的。但是对于数组来说，我们通常希望剩下的元素能够移动并占据被释放的位置。我们希望得到一个更短的数组。</p><p><a href="https://developer.mozilla.org/zh/docs/Web/JavaScript/Reference/Global_Objects/Array/splice">arr.splice</a> 方法可以说是处理数组的瑞士军刀。它可以做所有事情：添加，删除和插入元素。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arr.splice(start[, deleteCount, elem1, ..., elemN])</span><br></pre></td></tr></table></figure><h4 id="slice"><a href="#slice" class="headerlink" title="slice"></a>slice</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arr.slice([start], [end])</span><br></pre></td></tr></table></figure><p>它会返回一个新数组，将所有从索引 <code>start</code> 到 <code>end</code>（不包括 <code>end</code>）的数组项复制到一个新的数组。<code>start</code> 和 <code>end</code> 都可以是负数，在这种情况下，从末尾计算索引。</p><h4 id="concat"><a href="#concat" class="headerlink" title="concat"></a>concat</h4><p><a href="https://developer.mozilla.org/zh/docs/Web/JavaScript/Reference/Global_Objects/Array/concat">arr.concat</a> 创建一个新数组，其中包含来自于其他数组和其他项的值。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arr.concat(arg1, arg2...)</span><br></pre></td></tr></table></figure><h4 id="arr-forEach"><a href="#arr-forEach" class="headerlink" title="arr.forEach"></a>arr.forEach</h4><p><a href="https://developer.mozilla.org/zh/docs/Web/JavaScript/Reference/Global_Objects/Array/forEach">arr.forEach</a> 方法允许为数组的每个元素都运行一个函数。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">arr.forEach(function(item, index, array) &#123;</span><br><span class="line">  // ... do something with item</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>如果我们想检查是否包含某个元素，并且不想知道确切的索引，那么 <code>arr.includes</code> 是首选。</p><p>此外，<code>includes</code> 的一个非常小的差别是它能正确处理<code>NaN</code>，而不像 <code>indexOf/lastIndexOf</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">const arr = [NaN];</span><br><span class="line">alert( arr.indexOf(NaN) ); // -1（应该为 0，但是严格相等 === equality 对 NaN 无效）</span><br><span class="line">alert( arr.includes(NaN) );// true（这个结果是对的）</span><br></pre></td></tr></table></figure><p><strong>我们已经知道，箭头函数没有自身的 <code>this</code>。现在我们知道了它们也没有特殊的 <code>arguments</code> 对象。</strong></p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>学习 Vue 原理：响应式</title>
      <link href="/2021/05/30/xue-xi-vue-yuan-li-xiang-ying-shi/"/>
      <url>/2021/05/30/xue-xi-vue-yuan-li-xiang-ying-shi/</url>
      
        <content type="html"><![CDATA[<div class="note info"><p>近来在学习Vue，对于它的核心概念之一——响应式一直有所困惑，偶然间发现一门课程vue advanced workshop with Evan You。Vue的作者尤雨溪亲自讲解Vue。下面是对该课程学习的总结。欢迎大家参考和提出意见。</p></div><h2 class="bamboo-h " id="什么是响应式？">什么是响应式？</h2><br/><p>响应式是 Vue的一个核心特性，用于监听视图中绑定的数据，当数据发生改变时视图自动更新。</p><p>只要状态发生改变，系统依赖部分发生自动更新就可以称为响应性。</p><p>在web的场景下，就是不断变化的状态反应到DOM上的变化。</p><p>响应式实现数据驱动视图的第一步。</p><h2 class="bamboo-h " id="数据驱动视图">数据驱动视图</h2><br/><p>现在有这样一个例子：</p><blockquote><p>变量 a 和变量 b ，变量 b 的值永远等于 a 的 10 倍。</p></blockquote><p>如果使用命令式编程，可以很简单实现。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">let</span> b = a * <span class="number">10</span>;</span><br><span class="line"><span class="built_in">console</span>.log(b) <span class="comment">// 30</span></span><br></pre></td></tr></table></figure><p>但是当我们设置 b 的值为 4 时， b 还是等于 30</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">let</span> b = a * <span class="number">10</span>;</span><br><span class="line"><span class="built_in">console</span>.log(b) <span class="comment">// 30</span></span><br><span class="line">a = <span class="number">4</span>; <span class="comment">// 命令式，b不会保持关系同步</span></span><br><span class="line"><span class="built_in">console</span>.log(b) <span class="comment">// 30</span></span><br></pre></td></tr></table></figure><p>那么该如何实现当a改变时，b同时也改变呢？</p><blockquote><p>这里有一个神奇的函数onChanged()，它接收一个函数并且当 a 的值改变时，可以自动执行里面的代码，我们将 b 的更新放在里面，问题就解决了。</p></blockquote><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">onChanged (<span class="function">() =&gt;</span> b = a * <span class="number">10</span> ) <span class="comment">// 声明式, b 随着 a 改变而改变</span></span><br></pre></td></tr></table></figure><p>我们扩展一下，下面代码同样有一个神奇函数onStateChange，它会在 state 改变的时候自动运行，那我们只要在函数中编写dom操作的代码，就可以实现 dom 的自动更新了。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// DOM元素</span></span><br><span class="line">&lt;span <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;cell b1&quot;</span>&gt;&lt;/span&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 神奇函数，当state值改变会自动重新运行</span></span><br><span class="line">onStateChange(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.cell.b1&#x27;</span>).textContent = state.a * <span class="number">10</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>我们再进一步抽象，把 dom 的操作使用渲染引擎替换，但是我们不去研究渲染引擎的实现，只是简单的认为它会自动解析模版代码与数据关联即可，那代码就会变成下面这样。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// DOM元素</span></span><br><span class="line">&lt;span <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;cell b1&quot;</span>&gt;</span><br><span class="line">&#123;&#123; state.a * <span class="number">10</span> &#125;&#125;</span><br><span class="line">&lt;/span&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 神奇函数，当state值改变会自动重新运行</span></span><br><span class="line">onStateChange(<span class="function">() =&gt;</span> &#123; view = render(state) &#125;)</span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="如何实现响应式">如何实现响应式</h2><br/><h3 class="bamboo-h warning" id="getter 和 setter">getter 和 setter</h3><br/><p>Vue 中对象会被转换成响应式， 使用ES5的 defineProperty() 重写所有属性的 getter 和 setter 方法。</p><p><a href="https://developer.mozilla.org/zh-CN/docs/web/javascript/reference/global_objects/object/defineproperty">MDN上关于Object.defineProperty的介绍</a></p><p>下面将演示如何通过convert函数修改传入对象的getter和setter实现修改对象属性</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">convert</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line"><span class="built_in">Object</span>.keys(obj).forEach(<span class="function"><span class="params">key</span> =&gt;</span> &#123;</span><br><span class="line"><span class="keyword">let</span> internalValue = obj[key] <span class="comment">// 闭包，提供了存储机会 内部值</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, key, &#123;</span><br><span class="line">get () &#123;</span><br><span class="line"><span class="keyword">return</span> internalValue</span><br><span class="line">&#125;,</span><br><span class="line">set (newValue) &#123;</span><br><span class="line">internalValue = newValue</span><br><span class="line">&#125;</span><br><span class="line">&#125;)</span><br><span class="line">&#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="依赖跟踪（订阅发布模式）">依赖跟踪（订阅发布模式）</h3><br/><h4 class="bamboo-h red" id="为什么要依赖收集？">为什么要依赖收集？</h4><br/><p>先来看下面的代码</p><figure class="highlight plaintext"><figcaption><span>Vue(&#123;</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">    template: </span><br><span class="line">        `&lt;div&gt;</span><br><span class="line">            &lt;span&gt;text1:&lt;/span&gt; &#123;&#123;text1&#125;&#125;</span><br><span class="line">        &lt;div&gt;`,</span><br><span class="line">    data: &#123;</span><br><span class="line">        text1: &#x27;text1&#x27;,</span><br><span class="line">        text2: &#x27;text2&#x27;,</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>按照之前响应式中的方法进行绑定则会出现一个问题——text3在实际模板中并没有被用到，然而当text3的数据被修改（this.text3 = ‘test’）的时候，同样会触发text3的setter导致重新执行渲染，这显然不正确。</p><p>所以我们要进行<strong>依赖收集</strong>。</p><h4 class="bamboo-h red" id="如何实现？">如何实现？</h4><br/><p><strong>创建一个依赖跟踪类Dep，里面有两个方法：’depend’ ‘notify’。</strong></p><p>‘depend’: 收集这种依赖项。</p><p>‘notify’: 表示依赖发生改变，任何之前被定义为依赖的表达式、函数、计算都会被通知重新执行。也就是说我们需要找到一种让他们建立关联的方法。我们把这种计算关系叫依赖。这种计算也被认为是订阅者模式。<br>下面是Dep类期望达到的效果，调用dep.depend方法收集收集依赖，当调用dep.notify方法，控制台会再次输出updated语句。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> dep = <span class="keyword">new</span> Dep()</span><br><span class="line"></span><br><span class="line">autorun (<span class="function">() =&gt;</span> &#123;</span><br><span class="line">dep.depend() <span class="comment">// 实际上是把这个函数添加到订阅者列表中dep中,之后无论你在任何地方调用</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;updated&quot;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// should log: &quot;updated&quot;</span></span><br><span class="line"></span><br><span class="line">dep.notify()</span><br><span class="line"><span class="comment">// 函数再次被调用 should log: &quot;updated&quot; autorun</span></span><br></pre></td></tr></table></figure><p>这个 autorun 函数接收一个更新函数或者表达式，当你进入这个更新函数时，一切都变得特别，当代码放在这个响应区内，就可以通过dep.depend 方法注册依赖项。</p><p>代码实现</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">window</span>.Dep = <span class="class"><span class="keyword">class</span> <span class="title">Dep</span> </span>&#123;</span><br><span class="line"><span class="title">constructor</span> (<span class="params"></span>) &#123;</span><br><span class="line"><span class="built_in">this</span>.subsctibers = <span class="keyword">new</span> <span class="built_in">Set</span>()</span><br><span class="line">&#125;</span><br><span class="line">depend () &#123;</span><br><span class="line"><span class="keyword">if</span> (activeUpdate) &#123;</span><br><span class="line"><span class="comment">// 注册这个activeUpdate作为订阅者</span></span><br><span class="line"><span class="built_in">this</span>.subscribers.add(activeUpdate)</span><br><span class="line">&#125;</span><br><span class="line">&#125;,</span><br><span class="line">notify () &#123;</span><br><span class="line"><span class="comment">// 通知所有订阅者</span></span><br><span class="line"><span class="built_in">this</span>.subscribers.forEach(<span class="function"><span class="params">sub</span> =&gt;</span> sub()) <span class="comment">// 获取订阅函数并执行</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> activeUpdate <span class="comment">// 发布者</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">autorun</span> (<span class="params">update</span>) </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">wrappedUpdate</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">activeUpdate = wrappedUpdate <span class="comment">// 赋值给 wrappedUpdate 会使得当依赖关系发生改变update 函数会重新执行 动态更新依赖 保证依赖一直是最新的</span></span><br><span class="line">update()</span><br><span class="line">wrappedUpdate = <span class="literal">null</span></span><br><span class="line">&#125;</span><br><span class="line">updateWrapper();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">autorun(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">dep.depend()</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="实现迷你响应性系统">实现迷你响应性系统</h3><br/><p>结合前面两个函数convert() autorun() 将covert改名成observe()。<br>observer需要一个监听对象，监听他们得getter和setter，在getters和setters里面，我们可以设置依赖。<br>都整合后我们相当于创建了一个对象，我们访问一个属性，它收集依赖，调用dep.depend 当我们通过赋值改变属性值，调用notify触发改变。</p><p>期望实现的调用效果：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> state = &#123;</span><br><span class="line"><span class="attr">count</span>: <span class="number">0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">observe(state)</span><br><span class="line"></span><br><span class="line">autorun(<span class="function">() =&gt;</span> <span class="built_in">console</span>.log(state.count))</span><br><span class="line"><span class="comment">// should immediately log &quot;count is: 0&quot;</span></span><br><span class="line"></span><br><span class="line">state.count ++</span><br><span class="line"><span class="comment">// should log &quot;count is: 1&quot;</span></span><br></pre></td></tr></table></figure><p>最终整合代码如下：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dep</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="built_in">this</span>.subscribers = <span class="keyword">new</span> <span class="built_in">Set</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  depend () &#123;</span><br><span class="line">    <span class="keyword">if</span> (activeUpdate) &#123;</span><br><span class="line">      <span class="built_in">this</span>.subscribers.add(activeUpdate)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  notify () &#123;</span><br><span class="line">    <span class="built_in">this</span>.subscribers.forEach(<span class="function"><span class="params">sub</span> =&gt;</span> sub())</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">observe</span> (<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">Object</span>.keys(obj).forEach(<span class="function"><span class="params">key</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> internalValue = obj[key]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> dep = <span class="keyword">new</span> Dep()</span><br><span class="line">    <span class="built_in">Object</span>.defineProperty(obj, key, &#123;</span><br><span class="line">      <span class="comment">// 在getter收集依赖项，当触发notify时重新运行</span></span><br><span class="line">      get () &#123;</span><br><span class="line">        dep.depend()</span><br><span class="line">        <span class="keyword">return</span> internalValue</span><br><span class="line">      &#125;,</span><br><span class="line"></span><br><span class="line">      <span class="comment">// setter用于调用notify</span></span><br><span class="line">      set (newVal) &#123;</span><br><span class="line">        <span class="keyword">if</span> (internalValue !== newVal) &#123;</span><br><span class="line">          internalValue = newVal</span><br><span class="line">          dep.notify()</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> activeUpdate = <span class="literal">null</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">autorun</span> (<span class="params">update</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> wrappedUpdate = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    activeUpdate = wrappedUpdate</span><br><span class="line">    update()</span><br><span class="line">    activeUpdate = <span class="literal">null</span></span><br><span class="line">  &#125;</span><br><span class="line">  wrappedUpdate()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上都是基于个人的理解，写的学习笔记，欢迎大家提出建议。</p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>三种类型检查方法：typeof、instanceof 和 Object.prototype.toString</title>
      <link href="/2021/05/29/san-chong-lei-xing-jian-cha-fang-fa-typeof-instanceof-he-object.prototype.tostring/"/>
      <url>/2021/05/29/san-chong-lei-xing-jian-cha-fang-fa-typeof-instanceof-he-object.prototype.tostring/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="typeof 运算符,">typeof 运算符,</h2><br/><p>typeof 运算符返回参数的类型。当我们想要分别处理不同类型值的时候，或者想快速进行数据类型检验时，非常有用。 它支持两种语法形式：</p><ul><li>作为运算符：typeof x。</li><li>函数形式：typeof(x)。</li></ul><p>括号有的时候时必须的，如果不加上括号将会因为优先级的问题得不到我们想要的结果。例如：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typeof</span> <span class="string">&#x27;string&#x27;</span> === <span class="literal">true</span>; <span class="comment">// false</span></span><br><span class="line"><span class="keyword">typeof</span> (<span class="string">&#x27;string&#x27;</span> === <span class="literal">true</span>); <span class="comment">// &quot;boolean&quot;</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>对 typeof x 的调用会以<strong>字符串</strong>的形式返回数据类型：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typeof</span> <span class="literal">undefined</span> <span class="comment">// &quot;undefined&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="number">0</span> <span class="comment">// &quot;number&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="number">10n</span> <span class="comment">// &quot;bigint&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="literal">true</span> <span class="comment">// &quot;boolean&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="string">&quot;foo&quot;</span> <span class="comment">// &quot;string&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="built_in">Symbol</span>(<span class="string">&quot;id&quot;</span>) <span class="comment">// &quot;symbol&quot; </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="built_in">Math</span> <span class="comment">// &quot;object&quot;  (1) // 任何其他对象</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> <span class="literal">null</span> <span class="comment">// &quot;object&quot;  (2)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> alert <span class="comment">// &quot;function&quot;  (3) // 函数对象</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>最后三行可能需要额外的说明：</p><ol><li>Math 是一个提供数学运算的内建 object。</li><li>typeof null 的结果是 “object”。这是官方承认的 typeof 的行为上的错误，这个问题来自于 JavaScript 语言的早期，并为了兼容性而保留了下来。null 绝对不是一个 object。null 有自己的类型，它是一个特殊值。</li><li>typeof alert 的结果是 “function”，因为 alert 在 JavaScript 语言中是一个函数。在 JavaScript 语言中没有一个特别的 “function” 类型。函数隶属于 object 类型。但是 typeof 会对函数区分对待，并返回 “function”。这也是来自于 JavaScript 语言早期的问题。从技术上讲，这种行为是不正确的，但在实际编程中却非常方便。</li></ol><p>另外 typeof 处理包装类型的时候会返回对应的原始数据类型。但是不推荐这样写。例如：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">        <span class="keyword">typeof</span> <span class="built_in">Number</span>(<span class="number">1</span>); <span class="comment">// &quot;number&quot;</span></span><br><span class="line">        <span class="keyword">typeof</span> <span class="built_in">Boolean</span>(<span class="literal">true</span>); <span class="comment">// &quot;boolean&quot;</span></span><br><span class="line">        <span class="keyword">typeof</span> <span class="built_in">String</span>(<span class="string">&#x27;string&#x27;</span>); <span class="comment">// &quot;string&quot;</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h3 class="bamboo-h warning" id="typeof 运算符区别对待Object类型和Function类型">typeof 运算符区别对待Object类型和Function类型</h3><br/><p>从技术角度讲，函数在ECMAScript中是对象，不是一种数据类型。然而，函数也确实有一些特殊的属性，因此通过typeof 运算符来区分函数和其他对象是有必要的。</p><h3 class="bamboo-h warning" id="typeof 运算符对null的处理">typeof 运算符对null的处理</h3><br/><p>这个bug是第一版Javascript留下来的，javascript中不同对象在底层都表示为二进制，而javascript 中每种数据类型使用3bit表示，会把二进制前三位都为0的判断为object类型。</p><ul><li>000 表示Object类型的数据。</li><li>001 表示Int类型的数据。</li><li>010 表示Double类型的数据。</li><li>100 表示String类型的数据。</li><li>110 表示Boolean类型的数据。</li></ul><p>由于null代表空指针，大多数平台中值为0x00，因此null的类型标签就成了0，所以执行typeof时会返回 ‘object’。<br>% title h2, instanceof运算符 %}</p><p>% title h3,判断一个变量是数组还是对象, warning %}</p><p>首先我们试着用 typeof 运算符进行测试。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> arr = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> arr) <span class="comment">// &quot;object&quot;</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>发现输出结果为”object”，这并不是我们期望的结果。 所以使用 typeof 运算符并不能直接判断一个变量时对象还是数组类型。实际上，typeof 运算符在判断基本数据类型时会很有用，但是在判断引用市局类型时，却显得很吃力。</p><p>接下来我们试着用 instanceof 运算符进行测试。</p><p><strong>instance运算符用于通过查找原型链来检测某个变量是否为某个类型数据的实例</strong>。使用instanceof 运算符可以判断一个变量时数组还是对象。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">        <span class="keyword">const</span> a = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line">        <span class="built_in">console</span>.log(a <span class="keyword">instanceof</span> <span class="built_in">Array</span>); <span class="comment">// true</span></span><br><span class="line">        <span class="built_in">console</span>.log(a <span class="keyword">instanceof</span> <span class="built_in">Object</span>); <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> b = &#123;<span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span>&#125;;</span><br><span class="line">        <span class="built_in">console</span>.log(b <span class="keyword">instanceof</span> <span class="built_in">Array</span>); <span class="comment">// false</span></span><br><span class="line">        <span class="built_in">console</span>.log(b <span class="keyword">instanceof</span> <span class="built_in">Object</span>); <span class="comment">// true</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>有一点需要留意，a 同时还隶属于 Object 类。因为从原型上来讲，Array 是继承自 Object 的。instanceof 在检查中会将原型链考虑在内。 因此我们在判断一个变量时数组还是对象时，应该先判断数组类型，然后再去判断对象类型。如果先判断对象，那么数组值也会被判断为对象类型，这无法满足要求。</p><p>我们可以得到以下的封装函数。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">getDataType</span> (<span class="params">o</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (o <span class="keyword">instanceof</span> <span class="built_in">Array</span>) <span class="keyword">return</span> <span class="string">&#x27;Array&#x27;</span></span><br><span class="line">            <span class="keyword">if</span> (o <span class="keyword">instanceof</span> <span class="built_in">Object</span>) <span class="keyword">return</span> <span class="string">&#x27;Object&#x27;</span></span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;others&#x27;</span></span><br><span class="line">        &#125;</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h4 class="bamboo-h red" id="另一种思路：判断构造函数">另一种思路：判断构造函数</h4><br/><p>判断一个函数是否时数组或者对象，从另一个角度讲，就是判断变量的构造函数时Array类型还是Ojbect类型。因为一个对象的实例都是通过构造函数生成的，所以，我们可以直接判断一个变量的constructor属性。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">        <span class="keyword">const</span> a = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line">        <span class="built_in">console</span>.log(a.constructor === <span class="built_in">Array</span>); <span class="comment">// true</span></span><br><span class="line">        <span class="built_in">console</span>.log(a.constructor === <span class="built_in">Object</span>);  <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> b = &#123;<span class="attr">name</span>: <span class="string">&#x27;xx&#x27;</span>&#125;;</span><br><span class="line">        <span class="built_in">console</span>.log(b.constructor === <span class="built_in">Array</span>); <span class="comment">// false</span></span><br><span class="line">        <span class="built_in">console</span>.log(b.constructor === <span class="built_in">Object</span>); <span class="comment">// true</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>我们可以得到以下的封装函数。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">getDataType</span> (<span class="params">o</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">const</span> <span class="title">constructor</span> = <span class="title">o</span>.<span class="title">__proto__</span>.<span class="title">constructor</span> || <span class="title">o</span>.<span class="title">constructor</span></span><br><span class="line">            <span class="title">if</span> (<span class="params">constructor === <span class="built_in">Array</span></span>) <span class="title">return</span> &#x27;<span class="title">Array</span>&#x27;;</span><br><span class="line">            <span class="keyword">if</span> (<span class="title">constructor</span> === <span class="title">Object</span>) <span class="title">return</span> &#x27;<span class="title">Object</span>&#x27;;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;others&#x27;</span></span><br><span class="line">        &#125;</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h2 class="bamboo-h warning" id="Object.prototype.toString 函数">Object.prototype.toString 函数</h2><br/><h3 class="bamboo-h " id="使用 Object.prototype.toString.call() 方法来揭示类型">使用 Object.prototype.toString.call() 方法来揭示类型</h3><br/><p>每种引用类型都会直接或者间接继承自Object类型，因此它们都包含toString()函数。不同数据类型的toString()类型返回值也不一样，所以通过toString()函数可以准确判断值的类型。</p><ul><li>对于 number 类型，结果是 [object Number]</li><li>对于 boolean 类型，结果是 [object Boolean]</li><li>对于 null：[object Null]</li><li>对于 undefined：[object Undefined]</li><li>对于数组：[object Array]</li><li>对于对象：[object Object]</li><li>……等（可自定义）</li></ul><h3 class="bamboo-h warning" id="Symbol.toStringTag">Symbol.toStringTag</h3><br/><p>可以使用特殊的对象属性 Symbol.toStringTag 自定义对象的 toString 方法的行为。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123;</span><br><span class="line">  [<span class="built_in">Symbol</span>.toStringTag]: <span class="string">&quot;User&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line">alert( &#123;&#125;.toString.call(user) ); <span class="comment">// [object User]</span></span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>所以，如果我们想要获取内建对象的类型，并希望把该信息以字符串的形式返回，而不只是检查类型的话，我们可以用 {}.toString.call 替代 instanceof。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p><img "" class="lazyload placeholder" data-original="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/9bac10e554c14ea58d2dfdcd96b153d3~tplv-k3u1fbpfcp-zoom-1.image" src="https://img10.360buyimg.com/ddimg/jfs/t1/157667/29/9156/134350/603c6445Ebbc9cabe/41219c5d36d45072.gif" alt="总结"></p>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>javascript 求数组的最大值和最小值 (6种方法)</title>
      <link href="/2021/05/29/javascript-qiu-shu-zu-de-zui-da-zhi-he-zui-xiao-zhi-6-chong-fang-fa/"/>
      <url>/2021/05/29/javascript-qiu-shu-zu-de-zui-da-zhi-he-zui-xiao-zhi-6-chong-fang-fa/</url>
      
        <content type="html"><![CDATA[<h2 class="bamboo-h " id="通过prototype属性扩展min()函数和max()函数">通过prototype属性扩展min()函数和max()函数</h2><br/><p>算法1的思路是在自定义min()和max()函数中，通过循环由第一个值依次与后面的值作比较，动态更新最大值和最小值，从而找到结果。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 最小值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.min = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> min = <span class="built_in">this</span>[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">let</span> len = <span class="built_in">this</span>.length;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">1</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>[i] &lt; min) min = <span class="built_in">this</span>[i]</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> min</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.max = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> max = <span class="built_in">this</span>[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">let</span> len = <span class="built_in">this</span>.length;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">1</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>[i] &gt; max) max = <span class="built_in">this</span>[i]</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.min()); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.max()); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="借助Math对象的min()函数和max()函数">借助Math对象的min()函数和max()函数</h2><br/><p>算法2的主要思路是通过apply()函数改变函数的执行体，将数组作为参数传递给apply()函数。这样数组就可以直接调用Math对象的min()函数和max()函数来获取返回值。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Array</span>.min = <span class="function"><span class="keyword">function</span>(<span class="params">array</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.min.apply(<span class="built_in">Math</span>, array)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line"><span class="built_in">Array</span>.max = <span class="function"><span class="keyword">function</span> (<span class="params">array</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.max.apply(<span class="built_in">Math</span>, array)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Array</span>.min(arr)); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Array</span>.max(arr)); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="算法2的优化">算法2的优化</h2><br/><p>在算法2中将min()函数和max()函数作为Array类型的静态函数，但不支持链式调用，我们可以利用对象字面量进行简化。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 最小值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.min = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.min.apply(&#123;&#125;, <span class="built_in">this</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.max = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.max.apply(&#123;&#125;, <span class="built_in">this</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.min()); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.max()); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure><p>与算法2不同的是，在验证时，因为min()函数和max()函数属于实例方法，所以可以直接通过数组调用。<br>上面的算法代码中apply()函数传入的第一个值为{},实际表示当前执行环境的全局对象。第二个参数this指向需要处理的数组。<br>由于apply函数的特殊性第一个参数，指定为 null 或 undefined 时会自动替换为指向全局对象，原始值会被包装。所以我们也可以将第一个参数设置为null、undefind。</p><h2 class="bamboo-h " id="借助Array类型的reduce()函数">借助Array类型的reduce()函数</h2><br/><p>算法4的主要思想是reduce()函数不设置initialValue初始值，将数组的第一个元素直接作为回调函数的第一个参数，依次与后面的值进行比较。当需要找最大值时，每轮累加器返回当前比较中大的值；当需要找最小值时，每轮累加器返回当前比较中小的值。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 最小值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.min = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.reduce(<span class="function">(<span class="params">pre, cur</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> pre &lt; cur ? pre : cur</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.max = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.reduce(<span class="function">(<span class="params">pre, cur</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> pre &gt; cur ? pre : cur</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.min()); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.max()); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="借助Array类型的sort()函数">借助Array类型的sort()函数</h2><br/><p>算法5的主要思想时借助数组的原生sort()函数对数组进行排序，排序完成后首尾元素即是数组的最小、最大元素。<br>默认的sort()函数在排序时时按照字母顺序排序的，数字会被按照字符串处理。例如数字 18 会被当做”18”处理，数字 6 被当”6”来处理，在排序时是按照字符串的每一位进行比较的，因为”1”比”6”要小，所以”11”排序时要比”6”小。对于数值类型的数组来说，这显然不合理。所以我们需要进行自定义排序。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> sortArr = arr.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a - b)</span><br><span class="line"><span class="comment">// 最小值</span></span><br><span class="line">sortArr[<span class="number">0</span>]</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line">sortArr[sortArr.length - <span class="number">1</span>]</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(sortArr[<span class="number">0</span>]); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(sortArr[sortArr.length - <span class="number">1</span>]); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure><h2 class="bamboo-h " id="借助ES6的扩展运算符">借助ES6的扩展运算符</h2><br/><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 最小值</span></span><br><span class="line"><span class="built_in">Math</span>.min(...arr)</span><br><span class="line"><span class="comment">// 最大值</span></span><br><span class="line"><span class="built_in">Math</span>.max(...arr)</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Math</span>.min(...arr)); <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Math</span>.max(...arr)); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
